# Debrief — Competitive Ledger

**The purpose of this file is to make "belong in that company" a tracked gap instead of a mood.**

`MAINTAINING.md` sets the bar as "Debrief has to feel like it belongs in that company" and the
done-check asks each run to benchmark one surface against a mature tool. Until this file existed, that
benchmark went into a chat report nobody read and nothing accumulated. Now it lands here, one row at a
time, and the rows are where `ROADMAP.md` milestones come from.

## How to use it

1. **Every run adds or resolves at least one row.** The done-check requires it. A run that benchmarks
   a surface and files nothing here did not benchmark it.
2. **A row is a claim until verified.** Rows marked `UNVERIFIED` are leads, not facts. Verify against
   the tool's own documentation, a real device, or a real log before you build against one — and
   change the marker when you do, with what you checked. Vendor tools change with firmware, and a
   capability misremembered is an expensive thing to build against.
3. **`Verdict` is a decision, and decisions are cheap to reverse but expensive to re-derive.** Use:
   - `GAP` — they have it, we want it, it is not yet on the roadmap.
   - `QUEUED` — on `ROADMAP.md`, with the milestone named.
   - `HAVE` — we do this, at least as well. Say what proves it.
   - `BETTER` — we do it better. Say what proves it, because this is the claim that matters and the
     one most easily kidded about.
   - `REJECT` — deliberately not doing it. **Say why**, in one line. A rejection without a reason gets
     re-litigated every run.
4. **A `GAP` that survives two runs should become a milestone or a `REJECT`.** Standing gaps with no
   decision are how a competitive analysis turns into decoration.
5. **Format support is the one place to be maximally aggressive.** North Star #1 is "ingest anything".
   A `GAP` that is "we cannot read this vendor's file" is worth more than most feature rows, and the
   corpus is how it gets proven. Read published formats and specifications; never copy licensed code.

## The field

Debrief's competition is mostly **the software that ships with the altimeter a flyer already owns** —
which means the flyer already has it, it already reads their file, and it is free. Beating that
requires being better at something specific, not merely existing.

| Tool | What it is | Why it matters here |
|---|---|---|
| **Featherweight Interface Program** | Featherweight's desktop app for Blue Raven and Featherweight GPS | The benchmark for high-rate data and orientation. Blue Raven logs far more than altitude. |
| **AltusMetrum AltosUI** | Open-source desktop app for TeleMega / TeleMetrum / EasyMini | The benchmark for depth and honesty; open format, well documented, and a corpus source. |
| **Fluctus Control Center** | Vendor software for Fluctus flight computers | `UNVERIFIED` in detail — verify capability and format before building against this row. |
| **Eggtimer / PerfectFlite / Jolly Logic** | Vendor apps and utilities for widely-owned altimeters | Breadth of ownership. Whatever a beginner's first altimeter is, Debrief should read it. |
| **Excel / Google Sheets** | What flyers actually fall back to | The real incumbent. Anything Debrief cannot do, a flyer does in a spreadsheet — badly, but they do it. |
| **OpenRocket / RockSim** | Design and simulation | Not competitors, but the other half of the flyer's workflow: predicted versus flown is the comparison they want. Loft is the sibling that closes it. |

---

## Ledger

Newest first. `file:line` or a route where ours lives, so the comparison is reproducible.

| # | Tool | Capability | Where ours is | Verdict | Note |
|---|---|---|---|---|---|
| 45 | **OpenRocket** (VERIFIED from the `.ork` format's own simulation-status vocabulary) · RockSim | **Whether a design file can carry a MEASURED trace in the same element a predicted one lives in — and whether the reader can tell** | `lib/parsers/openrocket.ts:309` reads `status` off each `<simulation>` and carries it **verbatim and uninterpreted** into the cross-check note (`:452`, *"The design states this simulation as …"*). `PredictedRun.status`'s own docstring gives the reason: *"OpenRocket's format page shows one example value and defines none, so a rule keyed on it would be built on a vocabulary nobody has published"* | **`GAP`, and the premise the current behaviour rests on is now known to be wrong** | **Added 2026-08-14 by this run's competitive probe, aimed at the surface D10's last uncovered capability sits on.** OpenRocket's simulation status is not an open vocabulary: it is a closed enum, and one of its values is **`EXTERNAL`** — serialized `status="external"`, shown in the UI as *"Imported External Data"*, whose `<databranch>` is **always saved** and which the application refuses to re-simulate. **So an `.ork` can hold a flyer's own RECORDING in exactly the element Debrief reads a prediction out of.** Debrief would pair that design beside a log and label a measurement as *"what the design's simulator predicted"* — inverting measurement and simulation, which is the spine of this product rather than a labelling detail. Nothing in the corpus exercises it (the only `.ork` there states ordinary runs), so this is a reachable-in-the-wild gap rather than an observed defect, and it is filed rather than fixed for a reason that is genuinely the owner's: **the enum's values are published in GPL-3 source**, and reading seven identifier strings out of it is a licensing judgment the clean-room invariant does not settle on its own — a list of interface constants is arguably a fact about a format rather than code, and this repo has never had to decide that. Parked in `OWNER-NOTES.md`. **Where Debrief already leads:** it surfaces the design's own word to the flyer at all, which no vendor tool does when consuming somebody else's design, and it refuses to pick between several simulations rather than guessing (`predictionRefusal`). **`UNVERIFIED`:** whether RockSim's `.rkt` has any equivalent import-measured state; whether OpenRocket writes `status="external"` on export as reliably as it sets it in memory. **The action this row justifies:** if the owner permits reading the enum, `external` should suppress the "prediction" framing entirely and offer the branch as what it is — a second recording, which is D3's subject, not D9's. |
| 44 | **AltosUI/AltosDroid** (VERIFIED from `altoslib/AltosKML.java`) · Featherweight Interface Program · Blue Raven app · Google Earth · a handheld GPS | **What a TRACK export says about where its geometry came from — and whether either format's provenance field is used at all** | `lib/gps.ts#trackGpx` writes GPX 1.1 with `creator="Debrief"`, an optional `<metadata><desc>`, a navigable `<wpt>` at the last fix, and a `<trk>` of bare lat/lon (`<ele>` refused on purpose — GPX elevation is above the ellipsoid, Debrief's height is above the pad). `lib/gps.ts#trackKml` writes ONE `<Document>` with a `<name>`, one `<description>`, a landing `<Placemark>` and one `LineString` at `relativeToGround` | **`AHEAD` on the demonstration marker and on writing a GPX at all · `GAP` on the parsed provenance field, and on two instruments as two tracks** | **Added 2026-08-13, probing the surface D10 slice 5k just touched.** **Where Debrief leads, and one half of it is unique.** Neither GPX 1.1 nor KML 2.2 defines any flag for simulated, sample or demonstration data, and no tracked tool has the concept: `AltosKML.java`'s 354 lines carry nothing that distinguishes a replayed flight from a flown one. Slice 5k puts the made-up claim in **three** places in the GPX (`<metadata><desc>`, and the `<trk>` and `<wpt>` NAMES a handheld's go-to list shows) and **four** in the KML (all three names Earth draws, plus the one `<Document><description>`). Second lead: **Debrief writes a GPX at all.** The AltOS/TeleGPS documentation names CSV and KML as its two formats and mentions GPX nowhere, and the Featherweight app's export is documented as ".csv … and also a .kml"; AltosUI's KML Placemarks are flight-state markers for viewing, not a waypoint for walking. **Where it is behind, and this is the actionable half. (a) Both exports state provenance ONLY in human prose.** GPX 1.1 reserves `<src>` — annotated verbatim *"Source of data. Included to give user some idea of reliability and accuracy of data"*, legal on `trk`, `wpt` and `trkpt` — and KML 2.2 reserves `<ExtendedData><Data name>`, which Earth shows in the balloon by default. Debrief uses neither, and `creator="Debrief"` is a bare name where every other Debrief export carries a build stamp. That is row 41's own principle (the claim lives in a field the consumer already parses) unapplied to the two exports where a schema has already built the field. **(b) AltosUI draws BOTH altitudes and Debrief describes the second one.** VERIFIED from source: it emits two `<Folder>`s, `Baro` and `GPS`, each with its own coloured `LineString` at `absolute`, and the TeleGPS 1.8.3 note says the change was made *"to make it more useful for Tripoli record reporting"*. Debrief draws the barometric track and states the split in prose (`components/GroundTrack.tsx#kmlAltitudeNote`) — while `hasGpsAltitude` already tells the component the second altitude exists. Ours is the more honest single line (the two differ by 197–1,771 m on average across the nine corpus flights carrying both) and theirs is the more useful document. **(c) No time on any fix**, in either export: `trkpt` takes `<time>`, KML has a TimePrimitive and Earth's time slider, and Debrief writes neither. AltosUI does not either — its per-fix time and satellite count sit inside an XML **comment**, invisible to every parser — so this is a place to lead rather than catch up. **`UNVERIFIED`:** the Blue Raven app's KML internals (documentation only); RockSim entirely. **The action this row justifies — TAKEN the same day.** `<src>` on the `<trk>` and the `<wpt>`, `<ExtendedData>` on the `<Document>`, both carrying the board identity `flight.meta` already held, plus a real `creator` naming the build; `lib/logInfo.ts#recordedBy` takes only the keys that identify a recording and returns null when the file named none, so nothing empty is written. **What is left of this row is the two structural halves:** two altitude tracks in one KML (AltosUI's `Baro` and `GPS` folders), and a time on each fix — where AltosUI buries its own inside an XML comment, so that one is a place to lead rather than catch up. Row 43's CSV half is untouched. |
| 43 | **AltosUI/AltosDroid** (verified from this repo's corpus) · Featherweight Blue Raven · PerfectFlite · a spreadsheet | **What rides OUT with raw channel data — whether an exported CSV can still name the board and the flight it came from** | `lib/report.ts:836#analyzedDataCsv` and `lib/explore.ts#exploreCsv` write column names and numbers. `flight.meta` already holds serial / product / flight number (`lib/parsers/altosEeprom.ts:557`, `lib/parsers/altusmetrum.ts:189`) and reaches the canonical record (`lib/canonical.ts:254`) and **no other export** — `grep -n 'flight.meta' lib/report.ts` returns nothing, and `components/LogDetails.tsx:33` is `print:hidden` | **`GAP`, and the mechanism to close it is already decided in this repo** | **Added 2026-08-12, probing the surface D10 slice 5i touched.** **VERIFIED by reading corpus files rather than from memory:** AltosUI's own CSV export opens `#version,serial,flight,call,time,…` and repeats **serial and flight number on every data row** — `altusmetrum__issuiuc-endurance-20211030__TeleMetrum.csv` reads `6, 2098, 12,KD9LJW,…`, and `…irec_2023_telemega.csv` reads 10978 / 1. Device identity is the one metadata field the field does not omit, and it is carried per-record, not in a preamble. **That is the identical principle row 41 imported from NMEA/HL7/DICOM to settle where the made-up marker goes** — the claim lives in a field the consumer must already parse — and this run applied it to *"Debrief made this up"* while leaving *"which board recorded this"* on the floor. So `lib/documents.ts:85`'s standing objection (*a CSV has no comment syntax every reader agrees on*) does not bite: nobody is proposing a comment. **Where Debrief leads:** the exports carry a build stamp, an analyzed-at, the methods citations and now a provenance column, none of which any vendor CSV carries; and `analyzedDataCsv` writes bare numbers with units in the header, where PerfectFlite's StratoLogger is the only corpus CSV that puts a unit *inside* a cell (`76.7F`) — Debrief's own parser has to strip it. **`UNVERIFIED`:** whether Blue Raven's app-shared `.csv` carries any board id (its high-rate corpus file opens `Flight_Time_(s),Sync,Gyro_X,…` with none, but that is one export of one firmware); RockSim's export entirely. **The action this row justifies:** the data CSV takes the board identity the file already gave it, per row, on row 41's precedent — one increment, and it makes the cert-package artifact self-identifying. Filed in `BACKLOG.md` the same day rather than built, because slice 5i was mid-flight when it was found. |
| 1 | All vendor apps | **Shaped like an application** — a window with panes, not one scrolling document | `app/page.tsx` plus `components/Analyzer.tsx`; the analysis is one long page | `QUEUED` | P2 — Surfaces as routes. The largest structural reason Debrief reads as a page rather than a tool. |
| 2 | All vendor apps | **One consistent visual language**, because one team built one app | 50 components, **no shared primitive layer at all** — zero cross-component imports, 12+ card treatments, `text-xs` used 212× against `text-sm` 82× | `QUEUED` | P1 — One design system, adopted. Measured 2026-07-30. This is the most concrete instance of "assembled rather than designed" in either repo. |
| 3 | Featherweight | **High-rate data** — Blue Raven records far more per second than a baro-only altimeter, and the tool presents it without choking | Ingestion handles large logs; `UNVERIFIED` whether the ceiling and the plotting hold at Blue Raven's real rate | `GAP` | Verify with a real high-rate log first; the measurement is the increment. Decimation must never silently change a reported peak. |
| 4 | Featherweight | **3-axis orientation / attitude through the flight** — roll, angle off vertical, and what the rocket was actually doing | Nothing | `GAP` | High value and genuinely deep. Only honest where the log carries the channels; must degrade to "this board did not record it" rather than estimating. |
| 5 | AltosUI | **Reads its vendor's format completely**, including raw `.eeprom` | Shipped — D2, pinned by the raw `.eeprom` case | `HAVE` | Verified against the board's own software's interpretation, which is the standard to hold for every new format. |
| 6 | AltosUI · Featherweight | **The logger's own reported summary shown beside the derived one** | `components/DeviceSummary.tsx`, rendered by `components/FlightReport.tsx` on any flight whose file carries a summary | `HAVE` | **RESOLVED 2026-07-31 — the row was stale, not the feature.** It said "present it to the flyer, not just the test suite"; a Reading / Logger / Debrief / Agreement table has been on the report the whole time, and `e2e/device-summary.spec.ts` walks it. It also does more than state both figures: it judges agreement as *agree / consistent / differ* with the percentage, and carries a case a naive diff would call a disagreement — an accelerometer at rest reading 1 g, which Debrief reports as the force the airframe felt and the device subtracts, labelled "agree · exactly 1 g apart". Checked before writing this row rather than assumed. **Widened 2026-07-31:** it now also carries the board's own APOGEE and MAIN deployment shocks, which no barometric trace can recover — Debrief measures the same quantity on 19 of the 36 corpus flights that analyse, and on the rest the row says the reading is not comparable rather than going blank. |
| 7 | All vendor apps | **Reads only its own vendor's files** | Many formats into one model, plus a column mapper for any CSV | `BETTER` | The core structural advantage. Nothing else in the field reads a rival's file, and a flyer with two brands of altimeter has no other option. Under-sold outside the app. |
| 8 | FIP · AltosUI · DataCap | **Requires an install, often Windows-only** | Browser, offline, installable, no account, nothing uploaded | `BETTER` | Also a privacy claim a vendor cannot make: the file never leaves the device. `/privacy` says so; the landing surface does not. **Narrowed 2026-07-31:** not true of *all* vendor apps any more — Featherweight UI ships on iOS/iPadOS/macOS, so a Blue Raven owner already has a vendor app on the phone in their pocket. Verified on the App Store listing. |
| 9 | AltosUI · vendor apps generally | **Several recordings of one flight, reconciled and cross-checked** | Shipped — D3; stitching is D4, in progress | `BETTER` | **The claim was "nothing else does this" and that is refuted — narrowed 2026-07-31.** wFIP 2.0 loads several Featherweight device files onto one graph, booster-plus-sustainer among its own examples, so the OVERLAY is not unique. What survives is narrower and still real: reconciliation across *rival vendors'* files, with the disagreement shown rather than blended, and a refusal when the records cannot be aligned. Still `BETTER` against AltosUI, which has no multi-flight comparison at any version (absent from the v1.9.22.2 manual and from release notes 0.7.1–1.9.19). Say which tool the claim is against before it goes on a landing page. |
| 10 | Excel | **The flyer can compute whatever they want** | Fixed set of derived readings | `REJECT` as stated | A formula box would betray the provenance-first invariant — an unvalidated user expression cannot be labelled measured, derived or estimated. The right answer is depth (D7): more readings that are *checked*, each with its method. Say this rather than re-deciding it. |
| 11 | Excel | **Plots exactly what the flyer wants, in their colours, exported anywhere** | `reportProfile.ts` and `plotView.ts` carry order and hidden figures; `COMPARE_PALETTE` is hardcoded and caps at 6 | `QUEUED` | D5. The reason flyers still open a spreadsheet after using a vendor tool. |
| 12 | OpenRocket / RockSim / **Loft** | **Predicted versus flown, side by side** | **Reads a prediction as of 2026-08-04 (D9 slice 2); shows one nowhere yet (slice 3)** | `QUEUED` — D9 | Debrief holds the flight, Loft holds the prediction, and the flyer wants the overlay. Needs a decision about coupling — `MAINTAINING.md` says keep the tools distinct, so this is an import of a prediction, never a shared runtime. Owner-level; flag it rather than assume it. **This row used to say "the most valuable capability neither side of the suite has", which read as nobody having it. Corrected 2026-07-31:** see row 16 — an altimeter-side tool ships it today, so this is a deficit rather than an opening, and waiting costs more than the row implied. |
| 13 | Vendor apps | **Firmware-aware parsing** — the app knows which firmware wrote the file and reads it accordingly | D2 reads the file the card actually holds | `HAVE` | Hold this standard for each new format: read it the way the board's own software reads it, and prove it against the board's reported summary. |
| 14 | All | **A flyer can find the tool at all** | **`RESOLVED` 2026-08-09 with P5 slices 1–5.** README 4,545 words / 0 images → 1,948 / 4; the landing surface states what Debrief does that a flyer's own software cannot; the build is on screen and linked to its commit; a bug or a logger request is one click from inside the app; and `/changelog` says what each build did differently | `RESOLVED`, with one owner-level remainder | **Opened as `QUEUED`; resolved 2026-08-09.** The changelog half is worth recording because it goes PAST the field rather than reaching parity with it. OpenRocket keeps `ReleaseNotes.md` as one source and renders it in-app (`WelcomeDialog.java`), grouped by area and including computation changes — *"Clamp compressibility factor to avoid singularity at Mach 1"*, *"Fix CP calculation error with tail cones"* — but it states what was FIXED, never what your saved numbers should now read. AltOS ships release notes as Appendix H of the owner's manual. RockSim and a spreadsheet have nothing. Debrief's page leads each release with **Readings that changed**, which is the entry a flyer holding a March cert package needs and the one none of them writes, because none of them stamps the document with the build that produced it either (row 36). **What is left and why it is not closed here:** the repo's own description, topics and pinned links, which no session tool can write — parked under *Awaiting the owner* in `OWNER-NOTES.md`. |
| 15 | Featherweight (wFIP 2.0) | **Several device logs on one time-synced graph** — Blue Raven, Blue Jay, GPS Tracker and legacy Raven files loaded together; the release thread's worked example is a booster and a sustainer | `/compare` overlays recordings; `lib/stitch.ts` aligns per-stage logs and has no surface yet (D4) | `GAP` | **Amends row 9.** `UNVERIFIED` on the one point that decides how much it costs us: whether wFIP time-ALIGNS the files onto one clock or merely shares an axis. D4's differentiator is the alignment and the honest refusal, not the overlay — so verify this before claiming either. Sources reached were search snippets of TRF threads 190953/191414 plus the vendor's own one-line note; the forum returns 403 and the vendor's own host 503 through this proxy. |
| 16 | Featherweight (wFIP 2.0) | **Predicted versus flown inside the altimeter tool** — loads OpenRocket simulation files and overlays them on the flown traces | Debrief reads an OpenRocket `.ork` prediction as of 2026-08-04 (D9 slice 2); it does not yet overlay one | `GAP` | Filed separately from row 12 rather than folded in, because row 12 implied nobody had it. **Re-investigated 2026-08-04 and it STAYS `UNVERIFIED` — with counter-evidence now, which is a stronger statement than the original bare marker.** What was checked: the vendor's live capability page (`featherweightaltimeters.com/interface-program.html`, fetched successfully) **mentions OpenRocket, `.ork`, simulation, overlay and predicted-versus-actual nowhere at all** — its only capability text is downloading and displaying Raven/Raven2/3/4 data, plus "version 2.0 … also plots Blue Raven and Blue Jay data files that were downloaded through the phone". The vendor blog index enumerates **10 posts, newest 2023-07-07**, and neither software post names wFIP or OpenRocket. The two TRF threads that are this row's sole source still return **403**, and the Wayback Machine holds **zero snapshots of either**. So the claim rests entirely on second-hand search-result renderings of a forum thread nobody can currently open, against a vendor page that does not corroborate it. **Do not build against this row, and do not delete it either** — a firmware or release note could confirm it tomorrow. Treat D9 as "possibly already beaten by one competitor, unverifiably" rather than as either a clear opening or a settled deficit. |
| 17 | AltosUI | **The flyer controls the smoothing of baro-derived speed and acceleration**, from a `Configure Graph` tab that sits permanently beside the plot | Fixed Hampel window (`lib/analyze/index.ts`), explained on `/methods`, adjustable nowhere | `GAP` | Verified in the v1.9.22.2 manual and the 1.8.3 release notes. Read with row 10: a user-tuned filter that MOVES a reported peak would breach the provenance invariant, so if this is ever built it exposes sensitivity — it never re-labels a measured peak. |
| 18 | Featherweight UI (phone app) | **The post-flight surface is a phone at the range** — BLE download of a flight summary on landing, then the full log, with a configurable graph | P4 not started; `/` is a desktop page rescaled | `GAP` | Narrows row 8. The newest Featherweight hardware is not a Windows install; it is already on the flyer's phone before they leave the field. |
| 19 | MissileWorks mDACS | **The chart's visible items decide what the CSV export contains** | `lib/reportProfile.ts` deliberately refuses this: hiding a reading trims the *report*, never the data exports — `analyzedDataCsv` takes no profile and `compareJson` no hidden/order argument | `BETTER`, and the evidence is a competitor doing it the other way | **Verified from the vendor manual on 2026-07-31**, read directly rather than from a summary: *"This utility allows you to export the captured Flight Data to a secondary file using a standard Excel .CSV format. **The items specifically displayed in the chart are the items that are included in the export.** Time, Altitude and Pressure are always exported by default."* (<https://www.apogeerockets.com/downloads/PDFs/mDACS-usb-io-user-manual.pdf>, *Flight Data Export*). So an mDACS CSV is a picture of one screen, and two flyers who arranged their charts differently export different columns from the same flight. That is exactly the failure `reportProfile.ts` names — trimming a report is a presentation choice, trimming a data export is a broken file — and this is a shipped, widely-owned tool doing it. **Cite this rather than re-arguing the rule next run.** Same manual, same read: no colour customisation anywhere (zero occurrences of colour/color), and printing covers only the settings and legend sheets, not the chart — so mDACS has no image or chart-print output at all, which bounds how much of D5's remaining scope is competitively urgent. |
| 20 | Featherweight FIP · AltosUI · mDACS · Sheets | **Which FIGURES a saved document carries is the flyer's choice, and the same choice on every surface** | `components/FigureChooser.tsx`, shared by the single-flight report and the comparison; both exports read one `documentFigures` list | `HAVE` | Added 2026-07-31 with D5's first slice. The comparison used to ignore the choice and export a hardcoded altitude/velocity/acceleration, so the two surfaces disagreed about what the flyer had asked for and the Mach and dynamic-pressure overlays could not reach a document at all. **Closed out 2026-07-31, later the same run.** When this row was written, order and colour were still `GAP` against a spreadsheet and it said so. Both shipped with the rest of D5: `FigureChooser` carries `onMove` as well as `onColor`, so the flyer sets which figures a document holds, in what order, and in what colour — on the single-flight report and on the comparison, through one control and one stored choice. Colour is per **channel** on the report (`debrief.report.figureColors`) and per **flight** on the comparison (`debrief.compare.colors`), which is not an inconsistency: a report compares channels of one flight, a comparison compares flights on one channel, so the thing a colour has to stay attached to differs. The e2e assertions read the **saved SVG** rather than the swatch, because a colour that only changes the picker is exactly the bug worth catching. |

| 23 | AltosUI · Featherweight FIP/wFIP · Eggtimer · PerfectFlite DataCap · OpenRocket · RockSim | **Per-stage figures for a staged launch, read out of the FLIGHT LOGS** — each stage's own apogee, peak speed, peak acceleration, thrust-to-weight and burn, side by side | `components/StitchSurface.tsx` → *"What each recording read on its own"*, from `stageTiles` in `lib/readings.ts`; pinned by `corpus.test.ts` → *"every staged group reports per-stage figures"* over **9 recordings across the three staged corpus groups** | `BETTER`, and the field is empty rather than behind | **Added 2026-08-01 with D7 slice 4.** The claim is that no shipped tool does this from a recording, and each half was checked against a primary source rather than recalled. **AltosUI** opens exactly one flight record file per window — *"you are prompted to select a flight record file"* — its Flight Statistics are whole-flight only, and its per-"stage" tabs are a VOCABULARY TRAP: AltosDroid's *"separate tabs for each stage of your rocket flight"* means flight STATE (pad/boost/fast/coast/drogue/main/landed), which `altoslib`'s `AltosFlightStats` confirms in code. Its two-stage support is pyro CONFIGURATION only — separation charge after motor number, sustainer ignition on tilt and delay — with zero post-flight per-stage analysis, and live two-stage monitoring is one TeleDongle per stage in separate windows, never one clock (<https://altusmetrum.org/AltOS/doc/altusmetrum.html>). **Blue Raven** likewise puts staging entirely in config (a Vel2 > 300 ft/s gate, a tilt gate, burncount) and its device summary is apogee and max velocity per board, full stop (<https://www.featherweightaltimeters.com/uploads/1/0/9/5/109510427/blue_raven_users_manual_2025_sept_10.pdf>). **Eggtimer TRS** ships no plotting tool at all and instructs the flyer to open the CSV in Excel. **OpenRocket** has the closest structure in the field — a per-stage `FlightDataBranch`, one per booster — but from a SIMULATION, and its booster branch begins at separation with the pre-separation data missing from the export (maintainer-confirmed, <https://github.com/openrocket/openrocket/issues/2225>); it also has one global clock, so the alignment problem Debrief solves never arises for it. **RockSim** colours three flight tracks by stage role, again from a design. `UNVERIFIED`: that wFIP 2.0 time-ALIGNS several files rather than sharing an axis (vendor forum only; the forum 403s through this proxy), and whether RockSim's results summary carries per-stage result columns. **What this row does NOT claim, and the distinction is the safety-relevant one:** Debrief reports each RECORDING's own figures and combines nothing. There is no per-stage total, no summed impulse, and no staging mark — `EventType` still has no `separation` member, because one corpus record holds two separable burns and nothing yet distinguishes it from a single-motor flight with a corrupted stretch (D7 slice 4 carries the measurement). A tool that printed "stage 2 thrust-to-weight" off two boards that disagree by 12% would be worse than the gap. |

---

| 24 | Featherweight FIP 2.0 / phone app · AltosUI · Eggtimer · PerfectFlite · a spreadsheet | **A board's HIGH-RATE stream read beside the flight it belongs to** — 500 Hz gyro, accelerometer and the board's own quaternion attitude, on the same clock as the 50 Hz altitude log, from the two files the vendor's own downloader writes together | **Debrief now reads it** (`lib/highRate.ts`, `lib/parsers/blueraven.ts` → `highRateStream`), peak-preserving, with the standalone refusal untouched; pinned by `lib/highRate.test.ts` over all four corpus HR/LR pairs | `HAVE`, and the honest part is `BETTER` | **Added 2026-08-02 with D8 slice 1.** The vendor schema is VERIFIED from the Sept 2025 manual: `[sync code][gyro X/Y/Z deg/s ×100][Accel X/Y/Z Gs ×100][Quat X/Y/Z ×30000][Quat magnitude ×30000]` at 500 Hz, **no altitude and no absolute time** (<https://www.featherweightaltimeters.com/uploads/1/0/9/5/109510427/blue_raven_users_manual_2025_sept_10.pdf>) — which is exactly why every general-purpose reader refuses the file, Debrief included until this slice. **What the field does:** the Featherweight tooling plots its own high-rate file, and FIP 2.0 reportedly time-synchs parameters across files (`UNVERIFIED` — rocketryforum 403s through this proxy, search snippets only). **AltosUI names its orientation channels properly** and is ahead of Debrief there — `Roll Rate`, `Pitch Rate`, `Yaw Rate`, `Accel Along/Across/Through`, `Tilt Angle`, `Azimuth Angle`, body-frame rather than X/Y/Z (<https://raw.githubusercontent.com/optorres/AltOS/master/altoslib/AltosFlightSeries.java>) — and it *derives* tilt post-flight, duplicating the flight firmware's quaternion angle tracking inside the ground station (<https://keithp.com/~keithp/altusmetrum-sphinx/releasenotes.html>). **Eggtimer ships no analysis software at all.** **Where Debrief is now ahead, and it is a narrow, real lead:** no tool in this field reads a high-rate stream from one vendor beside a flight log while REFUSING to treat it as a flight in its own right, and none states what the reduction did to the samples. Debrief keeps, at each flight instant, the largest-magnitude sample the board actually recorded — measured, because the obvious linear resample loses **69.0%** of one corpus flight's 264 g lateral peak — and says so on the page. **Two things this row must NOT be read as claiming.** Debrief does not name the orientation channels semantically: `Gyro X` is the column, not `Roll Rate`, because at rest `lemiv` reads `Accel_X −0.99` and `jan10` reads `Accel_Z −1.00` — the board is mounted differently in different rockets, and mapping an axis to the airframe without knowing the mounting publishes a lateral reading as an axial one. That is D8 slice 2, and AltosUI is ahead until it ships. And Debrief derives no angle from the quaternions yet; slice 3 is where that is checked against the board's own `tiltAtBurnout`, which is a cross-check AltosUI has no equivalent of. **A genuine gap this probe found, worth its own row later:** Altus Metrum's EasyTimer has NO barometric sensor and AltosUI analyses its logs, so the field's leader already treats altitude as optional where Debrief treats it as mandatory (<https://altusmetrum.org/AltOS/doc/altusmetrum.html>). |

| 25 | AltosUI · Featherweight FIP / Blue Raven app · Eggtimer · PerfectFlite · a spreadsheet | **A board's own SOLVED ORIENTATION read out of the log it already wrote** — the roll angle the flight computer computed in flight, presented as the board's number with the board's stated limit, and told apart from a roll RATE | **Debrief now reads it** (`lib/parsers/blueraven.ts` → `rollAngle`, a `ChannelKind` of its own in degrees); all four app-CSV corpus low-rate files carry it and it had been parsed past. Pinned by `lib/parsers/blueraven.test.ts` → *"every app-CSV low-rate export yields the board's roll angle, matching its own column"* | `HAVE` on reading it; **body-frame naming RESOLVED 2026-08-02 for the axis** — `GAP` narrows to a derived tilt/azimuth series | **Added 2026-08-02 with D8 slice 2.** `Roll_Angle_(deg)` normalizes to `roll angle deg`, so the GENERIC importer's `\broll\b` rate test matched and a column of DEGREES arrived as a channel of degrees-per-second — the same defect `releaseAttitudeRoll` exists to stop, in the shape that guard cannot see (it needs `pitch` AND `yaw` siblings, and a tilt/future/roll board has neither). `lib/flight/columns.test.ts` was asserting it as correct. **Scope, corrected by review before shipping:** that path is the column mapper for an UNRECOGNISED file. `blueraven.ts` mapped no roll column at all beforehand, so no named-parser file ever published a wrong roll figure — the defect was reachable and latent, not observed. **The Blue Raven's roll angle is cumulative and unwrapped** — peaks of 26,099° on meraki, 24,240° on jan18, −4,969° on jan10 — which is why such a column read as a rate produces a figure no flyer could sanity-check. **On meraki it is also a FLOOR:** that file's board-measured `Roll Rate (HZ)` column holds at exactly ±6.38889 rev/s (2,300 °/s) for 46 of 36,700 samples — a sensor at its limit — and integrating that rate reproduces the board's stated angle exactly, 25,333° either way. So the method is confirmed and the total is a lower bound. **What the field does, VERIFIED from primary sources.** AltosUI names its orientation channels in the BODY frame — `Roll Rate`, `Pitch Rate`, `Yaw Rate`, `Accel Along` / `Across` / `Through` (<https://raw.githubusercontent.com/optorres/AltOS/master/altoslib/AltosFlightSeries.java>) — and it can, because Pad Orientation is a required board config it reads (`Antenna Up` = antenna forward along the flight path); `compute_orient()` creates its Tilt and Azimuth series ONLY when that config plus the accel and gyro zero-points are all present, which is a refusal of exactly the shape Debrief practises. The Blue Raven states the SENSOR frame explicitly — *"The gyro data is raw data in the sensor axis"* — so Debrief's `Gyro X` naming for the high-rate file is faithful to the vendor rather than lazy (<https://www.apogeerockets.com/downloads/PDFs/09170-blue_raven_users_manual_september_15.pdf>). **The finding that reopens D8 slice 1's closed question**: the same manual states *"The Blue Raven can be mounted in any orientation, and so it measures which direction, relative to its sensors, is the rocket axis by measuring the direction of the initial motion while the rocket is on the rail."* Slice 1 declined to map any axis to `accelAxial`/`rollRate` on the grounds that the mounting is unknowable — for this board it is knowable, and the board has already done it. That is the route into the rest of slice 2. **RESOLVED 2026-08-02, and the manual's method did not survive measurement.** Reduced to *which axis carries the largest excursion*, "the direction of the initial motion" separates the winner from the runner-up by only **1.1×–2.4×** over the four corpus high-rate files and picks the **WRONG** axis on two of them — at 500 Hz the lateral axes see shock and vibration that rival the boost. The board has its own inertial solution and more to go on than the log it writes; Debrief has the log, so it uses the part of the log that is unambiguous. **Gravity on the rail is that part.** A rocket stands within a degree or two of vertical, so the 1 g the accelerometer feels before it moves lies along the airframe: measured, the long axis sits **0.26°–1.72°** off the at-rest vector and outweighs the runner-up by **33.2×–216.4×**. Two corpus files rest on `X` and two on `Z` — the same board mounted differently in different rockets, which is the fact slice 1 correctly refused to guess at and this now measures instead. The window is the LAST still stretch before it moved rather than the first or the longest, because a rocket is often horizontal while it is prepared for longer than it then stands on the rail. So Debrief now labels the six sensor traces *roll rate* / *lateral rate* / *along* / *across the airframe*, which is the body-frame naming this row was opened against. **What stays `GAP`:** AltosUI goes one step further and derives a TILT and AZIMUTH series from the same channels (`compute_orient()`); Debrief names the axes and computes nothing off them, deliberately — a high-rate stream reaches it reduced to an envelope of the board's peaks, and a figure taken off that needs its own validation first. **Where Debrief is now ahead:** AltosUI can name its axes only because Pad Orientation is a required board CONFIGURATION it reads back, and it refuses the whole orientation series when that config is absent; Debrief takes the answer from the recording itself, so a file that arrives without any configuration still gets its roll trace named — and says nothing at all where the record cannot settle it (never left the ground, no still moment before it did, turning or rocking through it, or no axis within 15° of the gravity it felt). **What no tool in the field does, and Debrief now does:** carry the board's own stated limit with the channel. The vendor publishes it — the roll angle is *"a simple integration over time of the measured roll rate perpendicular to the rocket axis. It does not take into account how motion in other axes affects the rocket orientation"* — and neither the Blue Raven app nor AltosUI surfaces such a caveat beside the trace. **`REJECT`, with the reason, for the column beside it:** `Future_Angle_(deg)` is the board's projection of where its tilt is heading, used for its own tilt lockout. It is not a recording, and Debrief reports flights that were flown. **UNVERIFIED and not built against:** what the Featherweight Interface Program plots (the vendor page lists no channels), and the Blue Raven phone app's screens (that section of the manual is an empty placeholder — *"&lt;Description of phone app screens to be added&gt;"*). |

| 26 | AltosUI · Featherweight FIP · mDACS · Blue Raven phone app · a spreadsheet | **Reading a value off the chart at a chosen instant, ON A PHONE** — the thing a flight chart exists for, done with a thumb at the range | **Debrief now does it** (`components/Chart.tsx`), one finger anywhere on any of the six charts, and the reading STAYS after the finger lifts; pinned by `e2e/touch.spec.ts` → *"one finger reads a value off the chart, and the reading stays put"* | `LEAD` on both pointer and keyboard, and it is a structural one | **Added 2026-08-02, and it started as a defect in our own favour.** Debrief rendered a live legend on every chart and it never filled in under touch: uPlot drives its cursor off MOUSE events and every touch handler returned unless two fingers were down, so a phone showed `time — altitude —` for ever. Measured at 390 px with `hasTouch`: a one-finger drag left it empty while the identical drag through a synthetic mouse gave `time 47.14 s / altitude 6,402`. **What the field does, and why the lead is structural rather than clever:** AltosUI is a desktop Java application, FIP 2.0 is Windows-only and mDACS is Windows-only (<https://www.featherweightaltimeters.com/interface-program.html>, <https://www.apogeerockets.com/downloads/PDFs/mDACS-usb-io-user-manual.pdf>) — **none of them runs on a phone at all**, so none has a touch chart to be better or worse than. The only vendor software that does is the Blue Raven's own phone app, and its manual's screens section is an empty placeholder (*"&lt;Description of phone app screens to be added&gt;"*), so nothing about it can be sourced — `UNVERIFIED`, and not built against. A spreadsheet on a phone is the real incumbent and it is worse than either. **So the comparison that matters is against Debrief-as-it-was**, not against the field: the capability the field cannot have was advertised on our own surface and undelivered. Two details are what make it a real answer rather than a gesture handler: a horizontal drag claims the event and a vertical one does not, so the page still scrolls on the tallest surface in the app; and the reading persists after release, because a phone has no hover and a value that cleared on lift could never be read at all. **The keyboard half RESOLVED 2026-08-02, and the row's own claim about it was overstated and is corrected here.** It read "no `role`, `tabindex` or focusable element inside any plot": the `role="img"` and its `aria-label` were already there, on the plot host. What was actually missing is narrower and was verified before building — **no `tabIndex` and no key handling**, so the chart could not be focused at all, let alone read. The fix mirrors `GroundTrack`, which has had arrow keys, Home/End, PageUp/PageDown and Escape since it was built — so this was an inconsistency *inside one report* as much as a gap against the field. Arrow keys walk the samples in the **visible window** (Shift for ten at a time), Home/End take its ends, PageUp/PageDown step between the marks the chart already draws — liftoff, burnout, apogee, deployment — and Escape clears. It drives uPlot's own cursor, so the live legend a mouse user reads is the same element the keyboard fills in rather than a second reading rendered elsewhere, and only deliberate key presses write to the `aria-live` region — never a hover, which would queue an announcement per pointer sample. Pinned by `e2e/a11y.spec.ts` → *"a chart answers the keyboard, and says what it read"* and *"every chart is reachable by keyboard, not just the first"*, with three mutations run against them. **What the field does, VERIFIED:** AltosUI's graph is a JFreeChart panel with mouse zoom and a mouse-driven tooltip; nothing in its manual describes a keyboard readout, and the Windows-only tools cannot be checked from here — so this is `LEAD` on the same structural grounds as the touch half, not a claim that others tried and failed. **`GAP` narrows to the spreadsheet's real remaining edge:** arrow-key navigation of a numeric TABLE, cell by cell, which is row 22's territory rather than this one's. |
| 37 | OpenRocket · RASAero II · AltosUI · Featherweight / Blue Raven · Eggtimer · PerfectFlite · a spreadsheet | **A method write-up that CITES — a reader can follow a number to the published source it came from** | `lib/methods/content.tsx` — 51 blocks, ~12,700 words, 11 named groups, per-reading anchors, a popover at the number (row 34), 96 paragraphs, none over 400 words (P9 slice 4). **And zero sources: 0 URLs, 0 DOIs, 0 named external references and not one named algorithm across 102 KB.** Measured 2026-08-09 in this repo: `Hampel` 0, `Kalman` 0, `Barrowman` 0, `Savitzky` 0, `1976` 0, `http` 0 — while `Hampel` appears **11 times in the analysis code** and `1976` twice, so the code knows its sources and the page a flyer reads does not say them | **`RESOLVED` 2026-08-09, the same run it was opened — and it was a gap against this repo's OWN invariant before it was one against the field** | **Added 2026-08-09.** `MAINTAINING.md`'s CLEAN-ROOM invariant reads *"Implement every parser and method from published formats and sources **and cite them**"* — the citing half is being done in code comments, where no flyer can reach it. **What the field does.** OpenRocket is the high-water mark and the cautionary tale at once: its technical documentation began as Sampo Niskanen's Master's thesis at Helsinki University of Technology and carries a thesis's apparatus — VERIFIED 2026-08-09 from <https://openrocket.info/documentation.html>, which states that origin in as many words. It is a **separate PDF frozen at v13.05, dated 2013-05-10**, while the application is many releases past it — also verified from that page — so the field's best citation practice ships with a documented drift between the document and the code. (An opening-fan-out agent reported 281 numbered equations and a numbered bibliography in that PDF; I did not extract it myself, so that count is `UNVERIFIED` here and the row does not rest on it.) `UNVERIFIED` and reported by the same agent, recorded so a later run can check rather than re-derive: RASAero II's 154-page manual names Barrowman and Rogers Modified Barrowman repeatedly but carries no References section; AltosUI's manual has anchors and a numbered contents and no equations or bibliography at all. A spreadsheet cites whatever the flyer typed. **Why this is worth building rather than shrugging at.** Debrief's whole posture is that a number is a reading someone can check — it publishes error DIRECTIONS and corpus-measured ranges, which none of the field does (rows 12, 34) — and a reader who wants to check the method behind one has nowhere to go. The two halves are already in the repo and merely not joined: the algorithms are named in `lib/`, and the version stamp from row 36 is exactly the mechanism OpenRocket's frozen PDF lacks, so Debrief can state *which build's methods* a citation belongs to. **Shipped as P9 slice 5**, the same run this row was opened. Five sources, each FETCHED AND READ
before it was written down — the URL retrieved, the bytes confirmed to be a PDF, and the two the page leans on hardest checked to be the documents they are cited as (the Hampel paper's title block was extracted rather than trusted). `lib/methods/references.ts` opens with the reason: a fabricated citation is worse than none, because it puts a false claim of provenance on a safety-relevant number where it reads as diligence. **Where Debrief now differs from the field's best practice rather than merely catching up:** OpenRocket's bibliography is excellent and lives in a document frozen at v13.05 (2013-05-10) while the app is many releases past it, so a reader cannot tell whether a citation still describes the code — Debrief's citations sit in the same module the app renders, type-bound to the blocks that carry them, and a reference nobody cites fails the build. The version stamp from row 36 closes the other half: a document a flyer keeps names the build that wrote it, so a citation can be pinned to a specific state of the methods. **And the honest half is enforced as hard as the citing half:** 46 of 51 blocks cite nothing because they rest on no published method, and a test fails if any of them starts citing. The field's gap that remains open is RASAero II's and AltosUI's, not ours. |

## Standing conclusion — where Debrief actually wins

Keep this honest and current; it is what the landing surface and the README should say, and right now
they do not say it.

1. **It reads every board, not just the one that made it** — including any CSV, through the column
   mapper.
2. **It puts several recordings of one flight side by side** — *including files from different
   manufacturers* — and shows where they disagree instead of averaging them away. The cross-vendor
   part is what is unique; overlaying several of its OWN files is something Featherweight's tool now
   does too (row 15), so do not publish the older, broader claim.
3. **Nothing is uploaded, nothing is installed, nothing is paid for**, and it works at the range with
   no signal.
4. **A staged launch reads as a staged launch** — each stage's own apogee, speed, acceleration,
   thrust-to-weight and burn, on one clock, from the logs the flyer already has (row 23). Added
   2026-08-01. This is the one item on this list where the field is EMPTY rather than behind: the
   vendor tools put staging in configuration and analyse one file at a time, and the two tools with
   per-stage output get it from a simulation or a design. Publish it carefully — what Debrief does is
   put each recording's own figures beside each other and combine nothing, which is the part a rival
   would be tempted to skip.

Where it loses today: AltosUI derives a TILT and AZIMUTH series from a board's orientation
channels post-flight and Debrief computes nothing off them (row 24) — though as of 2026-08-02 a Blue
Raven's 500 Hz stream is READ, beside the flight it belongs to and without being treated as a flight
of its own, and its six sensor traces are NAMED by the airframe axis rather than by their column,
from the gravity the board felt on the rail rather than from a configuration a flyer has to have set;
no
predicted-versus-flown overlay (which an altimeter-side tool now ships — row 16), report export is
thinner than a spreadsheet, and no phone-first post-flight surface to match the one a Blue Raven owner
already carries (row 18). And, the one a flyer sees first, **it is shaped like a long page rather than
an application** — though as of 2026-07-31 it does at least have one shared set of primitives and a
test that keeps them, which is P1 in progress.
| 21 | Featherweight (FIP / phone app) · AltosUI · Eggtimer · a spreadsheet | **The vendor stamps the launch second into every file name, so one flight's files self-identify** — `BlRv_SN1537 HR_04-12-2025_12_45_49.csv`, `…LR_…12_45_49`, `…summary_…12_45_49_`, `GPSTrk05305_04-12-2025_12_45_50` — and the phone app downloads summary, low-rate and high-rate as one flight without the flyer stating anything | **Debrief now OFFERS the grouping** from that same stamp (`lib/proposeGroups.ts`, `components/GroupProposalBanner.tsx`) with the evidence in words, refusable, nothing merged without a press | `HAVE`, and it amends D6's premise | **Added 2026-07-31, and it changed the milestone it was filed against.** `ROADMAP.md` had concluded from measurement that no content signal separates one flight's files from another's — wall clocks are mostly absent, and apogee agreement admits more false pairs than true ones (the tightest agreement in the whole corpus, 0.28%, is between two files that are NOT one flight). That held for the three signals tested and missed the file NAME. Measured over the manifest's 61 files: **12 carry `MM-DD-YYYY_HH_MM_SS`, and a ±120 s rule over them yields 16 true pairs and 0 false pairs** — the separable distribution D6 concluded did not exist. It is the LAUNCH instant, not the download time, verified rather than assumed: the HR file's first row is `12:45:47.382` at `Flight_Time −2.040`, so T0 = 12:45:49.4, matching its own name. Bounds, both load-bearing: all 12 are Featherweight-ecosystem, so it NARROWS a proposal rather than replacing arrival — which is also why the staged booster/sustainer pairs are refused for a reason rather than by a special case, since none of their files is stamped; and the ground-station file sits 956 s from its siblings and must stay a MISS rather than be reached by widening. Contrast, verified from primary sources: **AltosUI does no grouping at all** — *"you are prompted to select a flight record file"*, one file, one graph (<https://altusmetrum.org/AltOS/doc/altusmetrum.html>) — though its names carry date, serial and flight number, a second key Debrief also ignores. **Eggtimer ships no analysis software**: one flight per browser click, one CSV, nothing to group with. And the spreadsheet baseline is *nothing* — the corpus preserves a real flyer having hand-reformatted Eggtimer Proton and Quantum data into Featherweight's column layout so one tool would plot them together, which is the incumbent workflow and the strongest single argument for D6 existing. **Measured and REFUSED 2026-08-01 — this clause used to read "still unused" and that was the wrong verdict.** Featherweight publishes an in-file join key — *"The sync code is a millisecond counter recorded in both the low-rate and high-rate data so that the two data sets can be aligned"* (<https://www.apogeerockets.com/downloads/PDFs/09170-blue_raven_users_manual_september_15.pdf>) — which `lib/parsers/blueraven.ts` quotes and declines. It cannot separate *one recording exported twice* from *two instruments*, and the measurement is decisive rather than close: `Sync` is a free-running millisecond counter mod 250, derived from the timebase, so any two files sharing a `Flight_Time` grid agree at SOME constant offset. The true pair `lemiv-HR × lemiv-LR` joins at **1 distinct offset, 100.0% over 9,655 shared samples** — and `lemiv-HR × reddit-HR`, two unrelated flights on different continents about nine months apart, joins at **1 distinct offset, 100.0% over 96,629**. The false join is the larger and cleaner of the two, and it is indistinguishable from the true one rather than merely admitted by a loose threshold. One genuine pair (`reddit-meraki2-121km`) has no `Sync` column at all. The vendor publishes it for time alignment WITHIN a recording, which is what the manual sentence actually says. The real per-recording identity in this data is the summary CSV's `Serial number` / firmware stamp, present on 2 of the 4 Blue Raven groups and already captured as `RecentFlight.summaryText`. |
| 22 | a spreadsheet · AltosUI · OpenRocket | **Tabular flight data you can work in** — every column at once, filter by value, select a range and copy just that, keyboard cell navigation, freeze both header row and first column | Was 7 tables; **2 sortable, 2 copyable, 0 keyboard-navigable, 0 filterable**, with the numeric sample table showing at most 6 channels because it inherited the chart's `MAX_SERIES` cap. Now **4 copyable**, the sample table shows **every channel the file holds**, and a `DataTable` primitive exists for the rest | **column cap `HAVE`** (D7 slice 1, 2026-08-01); copy-out `PARTIAL`; filtering and keyboard cell navigation stay `GAP` | **Added 2026-08-01.** Verified counts, taken over `components/` rather than recalled: exactly 7 `<table>` elements, no `role="table"`/`role="grid"` surface anywhere, so 7 is the whole population; `aria-sort` appears in `SampleTable` and `CompareView` only. What they have that we do not, all VERIFIED: a spreadsheet filters rows by value and copies an arbitrary selection, and Excel freezes header row and first column together where Debrief has `sticky top-0` on one table and `sticky left-0` on another, one axis each; OpenRocket lets you choose a CSV separator other than comma, which is wrong-by-default in decimal-comma locales, and writes an optional comments block naming flight events and field descriptions where Debrief's CSV first line is column names only; AltosUI writes `#` header lines carrying version and configuration. **UNVERIFIED:** that the Featherweight phone app shares data as `.xlsx` (search snippet only; the vendor page is silent), and PerfectFlite DataCap's `Data > Inspect` point list (forum only; perfectflite.com returned 503 through this proxy). What bounds how much of this is worth building, and it is more than it looks: **AltosUI has no numeric table of a RECORDED flight at all** — its tabs are Flight Graph / Configure Graph / Flight Statistics / Map, and the table view is live Monitor Flight only (VERIFIED against the manual). And **its CSV is always produced in metric**, regardless of the app's unit preference, where Debrief exports in the flyer's own per-quantity units with the unit named in every header. So the gap is against the SPREADSHEET, which is the real incumbent here, not against the vendor apps. **Two thirds resolved 2026-08-01.** The column cap is closed: the sample table has its own scope and defaults to every channel, measured beforehand over the corpus at **23 of 25 analysable logs carrying more channels than the chart draws, the richest 15, and 119 channels in total unreadable** without going back to the chart — and worse than the row implied, because the table inherited what was *plotted*, so a fresh Blue Raven LR read showed **1 of 11**, not 6 of 11. Copy-out went 2 tables to 4: `DESIGN.md` §5's `DataTable` now exists and both cross-check tables are on it, which is the first time either could be copied at all — the two surfaces §6 exists for, and the ones a cert document most wants to lift. **What remains is exactly what the spreadsheet still wins on** and it is worth stating precisely so a later run does not re-derive it: filter-by-value, copying an arbitrary cell selection rather than a whole table or a whole column, freezing both axes at once, and arrow-key cell navigation. None of the vendor apps has these either — this row's own finding is that AltosUI has no numeric table of a recorded flight at all — so the remaining `GAP` is against Excel alone, and it is a judgement about how far a browser tool should chase a spreadsheet rather than a deficit against the field. |
| 27 | Featherweight (Blue Raven app / FIP) · AltosUI · Eggtimer · PerfectFlite · a spreadsheet | **Saying that a download wrote the same samples twice** — a backup dump can repeat a block of a flight verbatim, and a tool that plots it silently redraws part of the flight | **Debrief now says it, on both halves.** The low-rate half has been reported as *"holds the same flight written twice"* since before this run; the high-rate stream is now scanned for verbatim repeated blocks (`lib/highRateRepeats.ts`) and the ones falling inside the stretch being read are named on the report and in every export | `LEAD` — RESOLVED 2026-08-03 from `GAP`, and the field has no position rather than Debrief being ahead of a competitor who tried | **RESOLVED 2026-08-03, on the second attempt the same day. The first was written, gated green and REVERTED before pushing** — the entry in `ROADMAP.md` under D8 carries the full account, and it is worth reading before anyone builds this. Measured over the four corpus high-rate files: `jan10` holds **three** row-0-anchored repeats totalling **27,261 rows**, `jan18` **44,793**, and `lemiv` and `meraki` hold **no repeated run of 50 rows or more at any offset**. **What makes this harder than it looks:** Debrief already truncates `jan10` to its first copy (analysis extent `0 – 20.22 s`, because its low-rate half is doubled as well), so its largest repeated block sits at flight clock ≈40 s, **outside the stretch the flyer is shown**, while a smaller block at ≈14.1 s is inside it. A statement about repeats is therefore only true if it is EXTENT-AWARE, and the extent is decided by the analysis long after the parser that can see the repeat. **What the field does — `UNVERIFIED` throughout, and not built against.** The Blue Raven's own phone app is the only software that reads this file shape and its manual's screens section is an empty placeholder (*"&lt;Description of phone app screens to be added&gt;"*); AltosUI, Eggtimer and PerfectFlite do not read the file at all, so none of them can mis-draw it. The field has no position here rather than Debrief being behind. **What shipped:** the copies are UNIONED (jan10's four blocks sum to 41,463 rows; the union is 27,261 — a row replayed twice is one row that is not its own instant), a stretch where the board simply sat still is not counted as a replay, and the note is EXTENT-AWARE — it names only the repeats inside the stretch the report draws, and says how many more lie outside it. That last clause is the one that made the first attempt wrong enough to revert: jan10's largest block sits at ≈40 s while the report draws 0–20.22 s, so the first version described a stretch the flyer could not see and stayed silent about the one corrupting the trace in front of them. Pinned by `lib/highRateRepeats.test.ts`, whose corpus assertions fail on both the un-unioned count and the un-filtered note. **Still `UNVERIFIED` and not built against:** what the Blue Raven phone app does with such a file — its manual's screens section remains an empty placeholder. |
| 28 | AltosUI · Featherweight FIP · Eggtimer · PerfectFlite DataCap · a spreadsheet | **Telling the flyer when their flight was NOT saved** — the storage the tool keeps flights in refuses a write, and the tool says so instead of carrying on as though it landed | **Debrief now reports the outcome of every write** (`lib/recents.ts` → `saveRecent` / `importLogbook` both await their transaction; `STORAGE_REFUSED` and `STORAGE_WRITE_REFUSED` are the two shared sentences; `useLogbook`'s `write-blocked` status stops the list promising to remember). Pinned by five e2e checks that abort the readwrite TRANSACTION the way a full quota does | `HAVE` — and explicitly **not** a lead, because this is a hazard the field does not have | **Added 2026-08-03.** The benchmark here is not a feature the vendor apps have and Debrief lacked; it is the reverse, and saying so is the point of the row. **Every other tool in this field writes to a filesystem.** AltosUI is a desktop Java application that saves flight records as files, FIP and mDACS are Windows desktop applications, Eggtimer ships no analysis software at all and the flyer's CSV is simply a file, and a spreadsheet saves a workbook. On all of them a failed write is an operating-system error the flyer cannot miss: a dialog, a red bar, a refusal to close the document. **Debrief keeps the logbook in IndexedDB, where a failed write is an asynchronous event that nothing has to listen to** — which is exactly what had happened. `saveRecent` assigned its id the moment the `put` was queued and `preventDefault()`ed the abort away; `importLogbook` resolved on `onabort` and returned the flight count regardless. So a full quota or Safari's ITP eviction produced *"Restored 12 flights."* over an empty logbook, and *"Added a.csv, b.csv to your logbook — tick them"* over a list with nothing to tick. **A desktop tool cannot have this defect, so there is nothing to be ahead of.** What the row records is that a browser tool inherits a failure mode the incumbents do not, that it is invisible unless deliberately handled, and that Debrief handles it now: the write reports its transaction's outcome, the copy separates *reads refused* from *writes refused* (a full quota reads perfectly, so a single sentence would have been half false), and the flight's ADDRESS survives a refused re-save because an aborted transaction rolls back and leaves the earlier row intact. **`UNVERIFIED`, and deliberately not built against:** whether the Blue Raven phone app — the one other tool in the field with browser-or-app storage rather than a filesystem — reports a failed save. Its manual's screens section is an empty placeholder (*"&lt;Description of phone app screens to be added&gt;"*), the same source gap rows 24, 26 and 27 hit. **The one surface still open**, filed rather than claimed: the analyze page shows a report without saying the flight was not kept, because the logbook list is not on the report screen (`BACKLOG.md`). |
| 29 | **Project APEX** (`apexrocketsim.com`) · OpenRocket · RockSim · AltosUI · Featherweight wFIP | **Predicted versus flown in one view** — a simulated trajectory and a real altimeter log on the same axes, with the gap stated | **Half, as of 2026-08-04.** Debrief now READS a prediction — `lib/parsers/openrocket.ts` opens an OpenRocket `.ork` and returns its ten stated figures as `ReportedValue[]` with `source: 'predicted'` — and refuses it as a flight. **It does not yet SHOW one anywhere**, so a flyer still cannot see the overlay this row is about; that is D9 slice 3. *This cell read "Nothing. Debrief reads no prediction format at all; `ReportedValue.source` is the single-member union `'device'`" until D9 slice 2 shipped and made both clauses false — updating it in the same run as the work, which is the discipline this file's own header asks for.* | `QUEUED` | **Added 2026-08-03, and it is the reason D9 was decomposed this run rather than later.** This row exists to name a competitor row 12 does not: **a free browser tool ships the mirror image today, on Debrief's own input format.** `apexrocketsim.com` (434 Aerospace) states verbatim: *"Import Blue Raven altimeter CSV files and overlay real flight data on simulated results."* — plus *"Back-calculates Cd from the coast phase"*, *"Free to use · No account required"*, and *"Free, browser-based, and built by the same people who fly HPR rockets. No signup. No ads."* It is a SIMULATOR that imports the log; D9 is an ANALYZER that imports the prediction. Same flyer, same question, opposite direction — and its Blue Raven CSV is a file Debrief already parses, so the flyer choosing between them is choosing which half they want to be good. **Licence not stated anywhere on its page; its physics documentation says only that the code is "open for inspection". Read rather than run, so treat every line above as a published claim.** **This row does NOT correct row 12, and a first draft claimed it did.** Row 12 was already corrected on 2026-07-31 and already says this is a deficit rather than an opening; the "most valuable capability neither half of the suite has" phrasing survived only in `ROADMAP.md`'s after-list line, which D9's decomposition has now replaced. Checking the row you claim to be correcting is the whole discipline of this file. **What the rest of the field does.** **OpenRocket cannot** — the only tracked request is issue #2356, open, no milestone, no assignee, whose asker's own words are *"compare actual flights with simulations to see how close I came to the sim"*, and its user guide documents export only. **RockSim cannot**, on Apogee's own evidence — *"One limitation with RockSim is that it can't graph out data from two simulations (or more) so that they can be compared"* (*Peak of Flight* #544). A first draft cited the RockSim v11 product page for "6-DOF sim, Launch Visualizer and Monte Carlo landing" and every clause of that was wrong: RockSim is 3-DOF (6-DOF is RockSim Pro), the page says "Landing Zone Prediction" not Monte Carlo, and Launch Visualizer is a separate product — filed at the time under "verified from primary sources", which it was not. **AltosUI cannot** — its manual describes selecting *"a flight record file, either a .telem file … or a .eeprom file"*, with no prediction format named anywhere (confirms row 23). **Featherweight is the one that may already beat this, and it must not be omitted from this row.** Row 16 records wFIP 2.0 as loading OpenRocket simulation files and overlaying them on flown traces. It stays `UNVERIFIED` — its sole source 403s through this proxy and the vendor page says nothing about OpenRocket — but "unverified, possibly already beaten" is the honest reading, not "nobody ships this". **The spreadsheet is the incumbent and it is real**: manifest file lines 42–43 preserve a flyer hand-reformatting Eggtimer Proton/Quantum into Featherweight's column layout so one tool would plot them together. **What blocks Debrief is not the capability, it is a file**: 61 corpus files, zero `.ork`, zero `.rkt`, zero manifest hits for `simulat|predict` — see D9 slice 1. |
| 30 | **RASAero II** (Chuck Rogers Aerospace) | **A published predicted-versus-flown comparison, as a table with a stated error** — `Rocket \| Motor(s) \| Diameter \| Flight Data \| Apogee Altitude \| RASAero Sim Apogee \| Percent Error` over 43 flights, with `Average Error = 3.47%`, `80.6% of Flights Error < 10%`, `41.7% < 5%`, and the `Flight Data` column naming the instrument each flown figure came from (barometric altimeter, GPS, optical track, integrated accelerometer) | **Debrief ships the same comparison at a finer grain, as of 2026-08-04 (D9 slice 3):** one row per READING of ONE flight — apogee, top speed, peak acceleration, Mach, time to apogee, flight time — where RASAero's is one row per FLIGHT on the single metric of apogee. Nothing in the field does the per-reading shape | `HAVE` (finer), with one deliberate divergence recorded below | **This is the only published predicted-versus-flown TABLE in the field and it was absent from this ledger entirely** (`grep -ni rasaero *.md` found it only in a `DESIGN.md` aside) — rows 12, 16 and 29 all discuss whether tools *can* overlay a prediction and none had looked at how one is actually laid out. Source: <https://www.rasaero.com/comparisons-alt.htm>. **The sign convention differs and it is worth knowing which way round.** Verified arithmetically from RASAero's own rows — MESOS 293,488 flown / 289,789 sim → −1.26%; Qu8k 121,478 / 116,254 → −4.30%; Blister 9,026 / 8,347 → −7.52% — RASAero states `(sim − flown) / flown`. Debrief states `(flown − predicted) / |predicted|`: **opposite sign and a different denominator**, so a flight RASAero prints as −4.30% Debrief prints as +4.5%, and at a large miss they diverge materially (flown 1,300 against predicted 1,000 is +30.0% in Debrief and −23.1% in RASAero's). Neither is wrong — RASAero is grading its own simulator, Debrief is reporting what a rocket did against what was expected of it — but a flyer holding both tables needs to be told. Stated on the methods page and under *Decisions taken without the owner*. **What RASAero has that Debrief does not:** a corpus-wide accuracy statement (`Average Error = 3.47%` over 43 flights) rather than a per-flight number. Debrief's own corpus could produce the equivalent the day more than one prediction is in it — filed as a `GAP` worth revisiting, not built on one fixture. **UNVERIFIED:** whether RASAero's single-flight pages overlay a predicted trace on a flown one; the comparison page itself is a table only. |
| 31 | AltosUI · Featherweight (Blue Raven app / FIP) · Eggtimer · PerfectFlite · OpenRocket · RockSim · a spreadsheet | **Thrust-to-weight off the pad, MEASURED from the flight log** — the "5:1 rule" number a range safety officer asks for, read from what the rocket actually did rather than from what it was designed to do | **Debrief publishes it, and as of 2026-08-04 publishes it correctly.** `lib/analyze/index.ts` differences the boost against the rocket's own resting reading, so the figure is convention-blind — a logger that reports acceleration net of gravity and one that reports specific force give the same ratio — and averages over **0.2 s taken off the clock** | `LEAD`, and the interesting half is the failure mode rather than the capability | **Added 2026-08-04, off the back of the Sev-1 that fixed it, and the row exists because the near-miss is more instructive than the win.** **What the field does, and the split is clean.** The two DESIGN tools compute thrust-to-weight and it is one of the first numbers they show — but from a motor's published thrust curve and an entered mass, before the rocket has flown; that is a different claim from measuring it, and neither imports a log to check it (row 29 records OpenRocket's inability at length). The ANALYSIS tools do not compute it at all: **AltosUI's manual does not mention thrust-to-weight anywhere**, and its Flight Statistics tab is documented only as *"Shows overall data computed from the flight"* with no enumeration of what that includes (<https://altusmetrum.org/AltOS/doc/altusmetrum.html>, verified 2026-08-04) — the tabs that do enumerate give *"maximum height, speed and acceleration"*. **Blue Raven's device summary is apogee and max velocity per board, full stop** (verified for row 23 against the vendor manual). **Eggtimer ships no analysis software**, and **a spreadsheet** gives whatever the flyer builds. So the flyer who wants the number they were asked for at the RSO table either takes it from a simulation of a flight that has not happened, or works it out by hand from a column of accelerations. **The failure mode, which is the part worth publishing.** Debrief had the capability and had it wrong, and wrong in a way no eyeball catches: the boost was averaged over a COUNT OF SAMPLES derived from the record's median interval rather than over a window of time. A flight log's rate is never one number — the pad is written slowly and the boost fast — so the window was 0.2 s on a uniform record and as little as 0.02 s on the rest, always short. **One device, one launch, two export formats: 4.98:1 from the `.csv` and 4.83:1 from the `.eeprom`, where the truth is 6.44:1 for both.** All 18 corpus flights that publish one read low. A tool that is alone in the field on a number is alone in checking it, and the check that caught this was not a reviewer's eye — it was two exports of one recording being required to agree. **UNVERIFIED:** whether the Blue Raven phone app computes a thrust-to-weight (its manual's screens section is the same empty placeholder rows 24, 26, 27 and 28 hit), and whether PerfectFlite DataCap's point inspector derives anything beyond the logged channels (perfectflite.com 503s through this proxy). |
| 32 | **Project APEX** (`apexrocketsim.com`, free, browser-based) · OpenRocket · AltosUI | **HOW a predicted trace and a flown trace are put on one set of axes** — which clock the x-axis belongs to, which of the two lines is dashed, and what happens when the two have different durations. Rows 12, 16, 29 and 30 all record *whether* a tool can overlay a prediction; not one of them records *how*, and D9 slice 4 has to make all three of those choices | **Debrief has not built the overlay yet — slice 4 is the next D increment — and the parser half landed 2026-08-05: a design's saved trace is read off its `databranch`, columns matched by name.** Slice 4's plan is a union x-axis keeping every original sample of each source with NaN elsewhere and `spanGaps` on, so neither line is resampled onto the other | `GAP`, and it corrects an assumption slice 4 was about to build on | **Added 2026-08-05. The nearest competitor does it the OTHER WAY ROUND, and that is the finding.** Project APEX is VERIFIED verbatim from its own advanced-feature guide: *"Run a simulation first so the time axis is established"*, and *"Measured data traces appear as dashed lines overlaid on simulation charts"*. So APEX gives the x-axis to the SIMULATION and dashes the MEASURED line — the exact inverse of slice 4's plan, which dashes the prediction and keeps the flight solid. Both are defensible and they encode opposite ideas about which trace is the reference: APEX is a simulator showing you how the flight compared to it, Debrief is an analyzer showing you how the prediction compared to the flight. **Debrief must not adopt APEX's convention** — `MAINTAINING.md`'s measurement-not-simulation spine makes the measured trace the reference here, and dashing it would say the opposite — but the divergence is now recorded rather than discovered later by a flyer who uses both. APEX also solves the duration mismatch with a *"Trim to sim / Full flight"* toggle rather than by truncating silently, which is a real affordance Debrief has no equivalent of and is worth stealing. **Row 16 gains a second, stronger counter-fact and stays `UNVERIFIED`:** the vendor's own current Blue Raven User's Guide (10 Sept 2025, 2.5 MB, fetched and text-extracted) contains **zero** occurrences of `OpenRocket`, `wFIP`, `FIP`, `overlay` or `predict`, and all 37 hits for `simulat` are the board's on-ground deployment-test mode — so "simulation" in Featherweight's own vocabulary is not a trajectory prediction at all. Both TRF threads still 403 through this proxy and the Wayback Machine holds no snapshot of either, so no primary source exists either way. **A naming hazard that falls out of the same reading:** if Debrief ever labels this feature "simulation", Blue Raven flyers already use that word for a ground test. **OpenRocket cannot overlay two of its OWN simulations** (issue #1485, open), and its author independently proposed *"different dash patterns for each"* — the same solution slice 4 reached. **AltosUI's Graph window offers four tabs and four Configure Graph options, none of them a second flight or a prediction** (<https://altusmetrum.org/AltOS/doc/altusmetrum.html>, verified 2026-08-05). |

| 33 | **The suite's own live sibling — `motor.fusionspace.co`** | **Shared persistent controls across the suite** — a flyer moving between the two tools meets the same theme control and the same tip control, in the same place, saying the same thing | Both live sites fetched and their rendered header markup compared, 2026-08-08. **The theme control already matches byte for byte**: `title="Theme: System (click to change)"`, `aria-label="Color theme: System. Click to change."`, glyph `◐`, label `System`, `rounded-md border-zinc-300 bg-white text-zinc-700 text-xs` — a three-way cycle on both, not the two-way toggle the sibling's own instructions text describes. Debrief's routes through `Button size="sm"` (`components/ThemeToggle.tsx`) where the sibling hand-rolls the classes, which costs 2 px of padding and gains a `pointer-coarse:min-h-11` floor the sibling does not have. **The tip control differs in colour only** (`components/KofiButton.tsx`): sibling `amber-300/50/700`, Debrief neutral secondary — geometry, icon, label and `title` string already identical | `HAVE` on the theme control · **`REJECT` on converging the tip button onto amber**, with the reverse convergence proposed | **Added 2026-08-08 from owner note `ON-B1`.** The note reads "the theme control button and the tip button is not consistent with the motor finder one" — half of that is measurably already false, and the half that is true has a reason. `DESIGN.md` §2 gives amber the meaning `warn` and permits `indigo` as the single accent; the same fetch shows the sibling's header ALSO carries a **sky** `API` chip (`border-sky-300 bg-sky-50 text-sky-700`), so on the shared design system it is the sibling that has drifted, not this one. Converging Debrief would import two off-system accents and spend the caveat hue in a persistent header that sits above every report a flyer scans for it — a cost a motor catalogue does not pay. The consistent outcome at no cost is the sibling adopting neutral; only the owner can make that change, so it is parked in `OWNER-NOTES.md` and recorded under *Decisions taken without the owner*. Reversal is one `className`. |

| 34 | AltosUI · Featherweight FIP / Blue Raven app · PerfectFlite · Eggtimer · OpenRocket · a spreadsheet | **In-app explanation of a computed value — "what is this number, and where can it be wrong", answered where the number is** | `components/MetricGrid.tsx` → `Popover` over `lib/methods/content.tsx`: every reading whose name is a term of art opens its full method write-up in place, with the caveats and the corpus-measured error ranges, and the flyer keeps their scroll position. 51 blocks, ~12,700 words, one module rendered by both the popover and `/methods`. Before 2026-08-08 all 21 opened a second browser tab | **`HAVE`, and it is a lead rather than parity** | **Added 2026-08-08 with P8 (owner note ON-3).** The vendor apps document their numbers in a **PDF or a web manual that is not the app** — AltosUI's is the AltOS manual at <https://altusmetrum.org/AltOS/doc/altusmetrum.html>, a separate document (verified 2026-08-05 for row 32); PerfectFlite and Eggtimer ship printed/PDF manuals. `UNVERIFIED` whether the Featherweight Interface Program or the Blue Raven phone app has any in-app help beyond tooltips — vendor tools change with firmware and this was not driven. OpenRocket has genuine in-app help and component tooltips, but it is a SIMULATOR: its help explains what to enter, not how a reading off a real flight was derived or where it is soft. A spreadsheet has whatever the flyer typed in a comment. **The distinction Debrief can claim and none of them can is that the explanation and the number come from one place**: the same module states the method, the refusal reason and the measured error direction, so a caveat cannot appear on the tile and be missing from the write-up. The gap to watch is the reverse one — eight standalone estimator panels and the comparison table still have no `?` at all (`BACKLOG.md`), which is where a competitor's uniform help would beat a partial one. |
| 35 | AltosUI · Featherweight FIP · OpenRocket · mDACS · a spreadsheet | **Exporting a flight and reading it back** — the tool's own export, re-imported, is the same flight | `lib/canonical.ts` → `lib/parsers/canonical.ts`: "Save record" on the report's export strip writes every sample of every RECORDED channel in canonical SI, and a parser registered FIRST in `PARSERS` reads it back. Verified over **50 corpus recordings and 9 committed fixtures**: identical field-by-field and to an identical analysis digest | **`HAVE`, and the shape of it goes past the field** | **Added 2026-08-08 with D11 slice 1.** A spreadsheet round-trips its own CSV exactly and the vendor apps re-read their own native file, so the bar here was parity. Before today Debrief had **seven export paths and zero import paths** — five were not even offered by the picker, and the one that was (`-debrief.csv`) re-imported through the column mapper as a materially different flight. What Debrief has instead of parity is a round-trip that is **provenance-preserving by construction**: the file carries the measurement and never the reading, so re-opening it re-analyses from scratch and an archived flight gets every later improvement rather than being frozen at the version that wrote it. Deliberately not a second source of truth. **The residual gap was closed the same day by slice 2, and the resolution is a refusal rather than a second round-trip**: `analyzedDataCsv` (`lib/report.ts:779`) re-imported as a different flight — its derived `velocity`/`acceleration` columns are re-read as MEASURED channels and its `dynamic pressure (kPa)` header claims the pressure role, blocking the recorded `Pressure (Pa)`. Reproduced through the real pipeline on `altusmetrum-telemetrum.csv` at **+37.9% on peak acceleration**; across the corpus the scoping probe measured **19 of 48 shifting it, worst +41.4%, and 16 flipping velocity provenance**. It is now recognised and explained rather than silently re-read, and the explanation points at the flight record. **That distinction is the part none of the tracked tools has**, and it is structural rather than a feature: their data export IS their round-trip format, so none of them can say *this file is the reading, that file is the recording* — a spreadsheet re-reads its own CSV exactly because the CSV is all there ever was. Debrief now writes two files with two jobs and refuses to confuse them. |
| 36 | AltosUI | **Which VERSION of the methods produced this document**, carried by the document itself | **Shipped 2026-08-08 with D11 slice 4.** `lib/buildInfo.ts` is inlined into the bundle by `next.config.mjs`, which reads the `public/version.json` that `scripts/stamp-version.mjs` was already writing — and which, until that day, **nothing in `app/`, `components/` or `lib/` read**. Carried by the .txt, .md and .html reports, the analysis .json and the canonical flight record | **`HAVE`, opened and resolved the same run** | **Added 2026-08-08.** The twin of row 34, and it points the other way. Row 34 makes 51 method blocks reachable from the number itself and calls that a lead — but the artifact a flyer KEEPS names none of it: not the `.csv`, not the `.json`, not the `.md`/`.html` report, not the new flight record. AltosUI's CSV writer stamps its own version (`AltosCSV.java`), so a file it wrote can be traced to the code that wrote it. A cert document filed in March and questioned in June currently has no way to say which methods produced its numbers — and Debrief changes those methods most weeks, which is exactly what makes the stamp worth more here than there. Cheap, and it was: one config read, one 40-line module, five call sites. **Two decisions are worth keeping.** It is inlined at BUILD time rather than fetched at runtime, because these documents are written in a browser that may be at a launch site with no signal — a `fetch('/version.json')` would make the one part of an export that needs a network. And the data CSV deliberately does NOT carry it: a CSV has no comment syntax every reader agrees on, a leading `#` breaks a spreadsheet's column detection, and that export exists to be pasted into one — the .json beside it in the same ZIP carries the stamp, so the bundle is traceable and the CSV stays clean. Pinned by `lib/buildInfo.test.ts`, which enumerates the documents so a seventh export that forgets fails, and which asserts the stamp never grows a word like "validated" — an identifier says which code ran, never that the numbers were right. |
| 38 | **OpenRocket** · RockSim · AltosUI · Featherweight FIP · a spreadsheet | **WHICH simulation a design's prediction came from, and whether that simulation is still current** | **Shipped 2026-08-09 with D9 slice 3b.** `lib/predictionChoice.ts` + `components/SimulationChoice.tsx`: a design stating several simulations is still REFUSED by default, and a flyer who knows which flew names it — each run labelled with its own name, the apogee it predicted, and the design's own status word, shown verbatim | **`PARITY` on the choice, `HAVE` on the attribution** | **Added 2026-08-09 and resolved in the same run, and the useful half is what the probe found wrong with the first version.** **What the field does, verified from source:** OpenRocket never picks silently — its Flight Simulations table lists every `<simulation>` with its motor configuration and a per-row status icon whose tooltip is one of *Up to date* / *Loaded from File* / *Out-of-date* / *Imported data* / *Not simulated* / *Can't run, no motors assigned* (`Simulation.java`, `Icons.java`, `messages.properties`). The flyer selects exactly one row and then plots or exports it, and **cannot plot two**: *“Allow multiple simulations in same plot”* has been open since 2022-06-25 (#1485, unassigned). RockSim cannot graph two simulations at all — Apogee's own newsletter tells the flyer to export to a spreadsheet. AltosUI has **no concept of a prediction whatsoever**: zero occurrences of `simulat`/`predict`/`OpenRocket` in the owner's manual. `UNVERIFIED`, recorded so a later run can check rather than re-derive: Featherweight's FIP 2.0 reportedly loads OpenRocket simulation files. A spreadsheet has no notion of “several simulations” — the flyer pastes whichever column they exported and nothing records which. **Where Debrief lands.** Parity on the mechanic and ahead on the sentence: OpenRocket's picker is a picker, while Debrief's says **whose statement the pick is** — *“You said … Debrief cannot read that from a flight log”* — and names how many runs were NOT compared. That is the measurement-not-simulation spine reaching a control, and no tracked tool has an equivalent because none of them is comparing a prediction against a flown log in the first place. **The probe's actual find, and it was a defect in this repo's own new surface:** `PredictedRun.status` was parsed, carried onto the offer, and rendered ONLY in a `title=` — a hover, which is nothing at all on the phone `DESIGN.md` §8 is written for. So a flyer could pick a run the design had been edited past and be compared against numbers predating the edit, silently. Fixed before the push: the status is visible text on every chip. **Deliberately NOT interpreted**, and that is the row's standing note — the format page shows one example value and defines none, so even *“warn unless it says uptodate”* would be a rule built on a vocabulary nobody has published. Debrief repeats the file's word and leaves the reading to the flyer. **Test hole, recorded rather than papered over:** all five runs in the corpus's `openrocket__example-simple-model-rocket__A-simple-model-rocket.ork` are `status="uptodate"`, so nothing exercises the stale path; sourcing or synthesizing a fixture that does belongs with this row. |
| 39 | **AltosDroid** (AltusMetrum's own Android app) · Featherweight FIP / Blue Raven app · Eggtimer WiFi · Jolly Logic AltimeterThree · PerfectFlite · a spreadsheet | **Reading a saved flight on the phone that is already in your hand at the range** — not a desktop layout narrowed until it fits, but the numbers laid out to be read down a 390 px screen | **Shipped 2026-08-09 with P4 slices 2 and 3.** `components/CompareView.tsx` and `components/ChannelExplorer.tsx`: one table that changes SHAPE below `sm` — each row becomes a labelled block with the column names moved inline, rather than a wide grid with a scrollbar under it. Pinned by `e2e/touch.spec.ts` at 390×844 | **`BETTER` on the reading surface — with a counter-note that undercuts most of the claim, below** | **Added and resolved 2026-08-09.** **What the field does, and the finding that matters is what NONE of them does:** apart from a spreadsheet, **not one of these tools will open another vendor's saved flight file on a phone at all** — so the surface Debrief just rebuilt has, strictly, no competitor to be worse than. Verified per tool: **AltosDroid** is the only genuinely phone-native tool in the set, and it is a *live telemetry* client — it talks to a TeleBT over Bluetooth and draws the flight as it happens; the AltusMetrum documentation lists downloading flight data to the phone as a future plan, not a shipped capability, so a `.eeprom` already on the phone has nowhere to go. **Featherweight**'s plotting lives in the Interface Program, which is **Windows-only**; the Blue Raven phone app is a board-configuration and live-status client, not a log reader. **PerfectFlite** ships no phone software of any kind — its `.fdf` files are read by a Windows utility. **Eggtimer WiFi** serves its own UI off the board over WiFi, which is phone-shaped but is the board's page about the board's own flight, not a file reader. **A spreadsheet** will open a CSV on a phone and is the honest incumbent here — and it is exactly the thing P4 exists to beat, because a 40-column log in a mobile spreadsheet is a horizontal scroll and nothing else. `UNVERIFIED`, and worth a later check rather than a re-derivation: the Eggtimer and AltimeterThree claims come from vendor documentation, not from a device in hand. **The counter-note, stated at full strength because the verdict is worthless without it:** *none of them needs Debrief to get the data OFF the board, and Debrief cannot do that for any board — no BT, no WiFi, no live telemetry, no continuity or deployment check. At the pad, the vendor app is the first app opened and Debrief is not opened at all.* Debrief's phone case is **post-recovery**, and P4's title — *“The range on a phone”* — reads wider than that, because a range day starts at the pad. (Checked before writing this: the *done when* itself is honestly scoped — *“drop a log straight off a card, read apogee and descent rate, check a deploy altitude, and show someone the result”* is all post-flight — so the overreach is in the framing, not in the acceptance clause. Said here rather than left as an impression, because the next run reads the title first.) Read it as scoping P4 rather than as a defect: the surface being built is the one a flyer uses walking back from the field with the rocket under one arm. **And the claim is two-thirds shipped, not shipped:** `ON-6` named three surfaces to answer “laid out vertically, not merely narrowed” against, and the third — `components/Chart.tsx`'s legends — is still the wide-viewport layout with its type scaled down. The row is `BETTER` on what exists and stays open on the legend. |
| 40 | **Featherweight wFIP 2.0** · OpenRocket · RockSim · AltosUI/AltosDroid · Featherweight UI / Blue Raven app · Eggtimer WiFi · Jolly Logic AltimeterThree · PerfectFlite · a spreadsheet | **What a flyer with no file, no board and no install can actually SEE the tool do** — demonstration data shipped with the tool | `lib/samples.ts` → three samples, all REAL public logs from `lib/parsers/__fixtures__/`: one flight read end to end, two boards recording one physical flight (Pnut + Raven, which Debrief reads at 1,025 ft and 1,029 ft — a 0.4% spread, **corrected 2026-08-14**: this row and the offer itself both said "≈1,009 ft", which is neither recording's reading), and a log beside its board's own stated summary. Offered from the analyze page and `/compare`, each with its provenance on the button | **`BETTER` on reach; the depth gap RESOLVED 2026-08-13, narrowed rather than closed** | **Added 2026-08-09, probing the surface D10 is about.** **What the field does, verified per tool:** **AltosUI/AltosDroid** ships nothing — Replay Flight works only on a file you downloaded off your own board (<https://altusmetrum.org/AltOS/doc/altusmetrum.html>). **Eggtimer WiFi** has nothing to ship: the UI is pages served by the board's own access point, so with no board there is no software to open (<https://eggtimerrocketry.com/eggtimer-quantum/>). **Jolly Logic AltimeterThree** is free and fully functional and records nothing without the altimeter (<https://apps.apple.com/us/app/altimeterthree/id924087924>). **Featherweight UI / Blue Raven app**'s "Sim" drives the board's own sensors and needs the board in hand. **OpenRocket** ships 13 example DESIGNS under File ▸ Open Example (<https://openrocket.readthedocs.io/en/latest/setup/getting_started.html>) — designs, not recordings. `UNVERIFIED`: PerfectFlite's download page 503'd through this proxy, so its absence of samples is inferred rather than read; RockSim's bundled `.rkt` count likewise. **The one rival that beats us, and by how much: Featherweight's wFIP 2.0 installer carries two real sample sets, one of them Jason Brown's two-stage *BadaBoom* — booster and sustainer, Raven4 + GPS Tracker + Blue Raven, three devices across two stages in one click.** That single sample demonstrates multi-device reconciliation (D3) and per-stage stitching (D4) together, and Debrief has no staged sample at all, so `/stitch` — a shipped milestone — is undemonstrable to anyone who has not brought two of their own files. `UNVERIFIED` on the sample details: the forum thread 403s through this proxy and the claim comes from search snippets, so confirm before building against the specifics. **Where Debrief leads, and it is structural rather than a feature:** every rival that ships samples ships them **inside a Windows installer**, and OpenRocket's and RockSim's are simulations. Debrief is the only one where a flyer with no file, no board and no install reaches a **real recording** in one click, in a browser, on a phone — with whose flight it is stated on the button. **RESOLVED 2026-08-13 — D10 (d)'s staged pair shipped, and `/stitch` offers it from its empty state.** Debrief now demonstrates per-stage stitching in one click, in a browser, with no install and no board, which is the half of this row that was a gap. **What is honestly still short, and it is the reason this reads `narrowed` rather than `closed`:** Featherweight's *BadaBoom* set is a REAL two-stage flight on three real devices, and Debrief's pair is two curves it made up and labels as such on every surface. A made-up pair demonstrates the FEATURE and cannot demonstrate the READING — nobody learns what a real staging transient looks like from it. Closing that needs a real staged log whose owner has licensed it for redistribution, which is a corpus problem rather than a code one and is not on the roadmap; the private corpus's staged record cannot ship. **Also still true and still ours:** every rival that ships samples ships them inside a Windows installer. |
| 41 | **OpenRocket** · Featherweight Blue Raven · *and, deliberately, outside rocketry:* **NMEA 0183** · **HL7 v2** · **DICOM** | **How a tool says that numbers it is showing were NOT measured** — the marker that stops simulated or demonstration data being read as a recording | `lib/synthetic.ts` → a `Synthetic` key in the file's own metadata block, read by key rather than by sentence, promoted to `flight.notes[0]`, and therefore carried through every persistence hop: the report, the readings grid, the logbook row and its backup, the .txt/.md/.html/.json reports and the canonical flight record | **`BETTER` than the rocketry field, `GAP` against the instrumentation world — and the gap names the next slice** | **Added and half-resolved 2026-08-09 with D10 slice 5a.** **The rocketry field is weaker than expected, which is the useful half.** OpenRocket's CSV export never says "simulated" at all: `CSVExport.java` writes the simulation's *name* and *status*, and the whole status vocabulary is *Up to date / Loaded from file / Out of date / Imported data / Not simulated* — no word means "this is predicted" (<https://github.com/openrocket/openrocket/blob/unstable/core/src/main/java/info/openrocket/core/file/CSVExport.java>). Worse, the comment block is behind a user checkbox and its comment character is an editable free-text field (<https://github.com/openrocket/openrocket/blob/unstable/core/src/main/resources/l10n/messages.properties>), so with comments off the export is a bare column dump indistinguishable from an altimeter log. Its internal vocabulary calls simulation output *flight data* (`FlightDataType`, `Noflightdata = No flight data available.`). Featherweight's Blue Raven runs on-board simulations from the phone app — *"the Blue Raven responds exactly as it would to a real flight"* (<https://www.featherweightaltimeters.com/blog/using-the-blue-ravens-phone-app-to-set-up-a-2-stage-flight-simulate-it-and-fly>) — and those runs share out as `.csv` on the same path as real flights; whether that file carries any marker is `UNVERIFIED` (the manual 429'd and the forum 403'd through this proxy). RockSim's export is `UNVERIFIED`. **So Debrief already leads here**: the claim is unconditional, cannot be weakened by editing the file (the reader matches the KEY and returns Debrief's own sentence), and rides the notes rather than a field, which is why it survives a saved record and a logbook backup. **The gap is against tools outside this hobby that have solved exactly this, and it is worth copying rather than admiring.** NMEA 0183 marks it **per sentence** — GGA quality indicator `8` is *Simulation mode*, and the FAA mode indicator carries `S` for *Simulated* in RMC/GLL/VTG (<https://gpsd.gitlab.io/gpsd/NMEA.html>). HL7 v2 makes it a **required header field on every message** — MSH-11 Processing ID, table 0103: `P` production, `T` training, `D` debugging (<https://hl7-definition.caristix.com/v2/HL7v2.5.1/Tables/0103>). DICOM's per-instance analogue is `(0008,0008) Image Type` value 1, `ORIGINAL` vs `DERIVED` (<https://dicom.innolitics.com/ciods/ct-image/general-image/00080008>). The shared principle is **per-record redundancy: the claim lives in a field the consumer must already parse to get the numbers at all**, not in a preamble. Debrief's marker is still one row above the header — and `AnalyzedTable.headerRow` skips that block by design, which is precisely the reader that would strip it. Select the data and paste it into a spreadsheet and the numbers travel naked. **The change this row justifies, and it settles a question `ROADMAP.md`'s D10 had left open as "a column or a decision":** the data CSV takes a per-row column rather than a header comment, on the NMEA/HL7 precedent. Queued as D10 slice 5b. |
| 42 | **AltosUI/AltosDroid** · Featherweight UI / Blue Raven app · Eggtimer WiFi · OpenRocket · RockSim · a spreadsheet | **Whether low-emphasis text — annotations, units, help affordances — is actually READABLE, and whether anything checks it** | Every ENABLED grey in `components` and `app` now clears WCAG AA in both themes, held by `lib/design-system.test.ts` *"meets WCAG AA in BOTH themes"*: ratios computed from hex, the arithmetic self-rated against 21:1 and 1:1 first, three narrow stated exemptions (`aria-hidden` decoration, genuinely `disabled` controls, `print:`-scoped text). It found eight real sites the day it was written — the metric grid's `?` at **2.56:1**, five icon glyphs at **2.46:1**, three run annotations at **3.67:1** in dark | **`AHEAD`, and the axis is the point rather than the score** | **Added 2026-08-11.** The comparison is uneven on purpose: the field is native desktop and phone software, where WCAG is not the governing standard and none of these tools publishes a conformance claim — so this is not "they fail and we pass". What is genuinely comparable is that **a dark theme is now table stakes in this field and a contrast regression is invisible to every check any of them runs**, including ours until this run: §9 had claimed contrast was covered by two commands, `e2e/contrast.spec.ts` and a `-t "class half of the dark variant"` test, and **neither existed in either repo**. The owner had already reported the SIBLING tool's docs as *"grey in dark mode, incredibly hard to read"* while every §9 count there sat at target, which is the same failure arriving from the other direction. `UNVERIFIED`: no vendor tool was driven for this row — AltosUI and the Featherweight/Blue Raven apps need the board in hand, and Eggtimer's UI is served by the board's own access point, so their low-emphasis text was not sampled. The claim here is about the CHECK, which is verifiable in this repo, not about their rendering. Worth revisiting if a session ever gets a board: the useful question is whether any of them offer a high-contrast mode at all. |

| 46 | **RASAero II** (Chuck Rogers Aerospace) · **Project APEX** (`apexrocketsim.com`) · OpenRocket · RockSim · AltosUI | **Shipping a DESIGN and a RECORDING of the same rocket together, so a stranger sees predicted-versus-flown with nothing of their own** | **Shipped 2026-08-17 with D10.** `/` offers *"A design, and the flight it flew"* — a generated `.ork` beside a generated log of the same rocket, reading Predicted 5,248 ft against a flown 5,467 ft in one click. | **BETTER**, and this is the row that says why it is worth claiming. **Nobody in the field ships the pair.** RASAero II publishes the strongest predicted-versus-flown artifact there is — two overlaid figures (altitude and velocity) with per-metric error beside them, `comp-flight-Proteus6.htm` stating apogee 2.04%, max velocity −3.25%, parachute descent time 6.22% — and ships **only the design half**: *"The RASAero II rocket file for this rocket (Proteus6.alx1) is included in the RASAero II software download."* The flown data on those pages came off a Blacksky AltAcc and is not downloadable. OpenRocket ships example designs with saved simulation data and **no recordings**; the analysis tools ship no recording at all (row 40). **Project APEX is the sharpest comparison, because it is the only other free browser tool with the overlay and it ships no demo of it**: its page advertises *"Import Blue Raven altimeter CSV files and overlay real flight data on simulated results"*, mentions no sample, example or demo anywhere, and per row 32 a visitor must *"Run a simulation first so the time axis is established"* — so seeing the overlay there costs a design AND a log. Debrief costs one click. **Verified this run** by fetching `rasaero.com/comp-flight-Proteus6.htm`, `comp-flight-Cal.htm` and `apexrocketsim.com`; the RASAero overlay claim also RESOLVES row 30's stated `UNVERIFIED` ("whether RASAero's single-flight pages overlay a predicted trace on a flown one") — they do, and it settles that **velocity, not only altitude, is expected in the comparison**, which is the next slice this row buys. The honesty cost is stated rather than hidden: both halves of Debrief's pair are made up and say so separately, because the alternative — an invented design against a real recording — would publish a fabricated error percentage about somebody's actual flight. The route that would beat this outright is a REAL design matching one of the six real flights already shipped; `github.com/ISSUIUC/ISS-STR` (the team whose flight is sample #1) states it holds *"OpenRocket, RasAero, and other structural design files"*, `UNVERIFIED` for contents and licence because the GitHub API is scoped away from that org this session. |
| 47 | **Featherweight FIP/iFIP** (VERIFIED, 2021 tracker manual) · **Eggtimer/Eggfinder LCD** (VERIFIED, LCD Users Guide 2.01c) · **AltosUI** (VERIFIED, AltOS docs) · MissileWorks RTx · a spreadsheet | **How good the fix behind a coordinate actually was — the satellite signal breakdown, the dilution of precision, and how high the last fix was taken** | **The AltOS dilution half is SHIPPED (2026-08-18).** `lib/parsers/altusmetrum.ts` reads `hdop`/`vdop`/`pdop` into three unitless channels, and the recovery view and every saved document state the horizontal spread behind the positions — a range and a middle, never one number, and never converted to metres. **The Featherweight signal half is still a GAP**: `lib/parsers/featherweightGps.ts:256-258` names the `>40`/`>32`/`>24` dB-Hz bins **in a comment** and pushes only the `#TOT` total as a `Satellites` channel | `GAP` on the Featherweight signal bins; **RESOLVED on dilution of precision**; `AHEAD` on what the fix is CALLED | **Added 2026-08-17 from the opening fan-out's competitive probe — and the probe's headline finding was REFUTED by measurement, which is the more useful half of this row.** The probe reported that a track losing lock at altitude still prints *"Landed from pad"*, offers *"Copy the landing coordinates"* and writes a GPX `<wpt>` named as a landing, on the strength of `lib/gps.ts:450` (*"last valid fix wins → the resting place"*). Measured across the corpus: **5 of the 12 recordings carrying lat/lon do end with their last fix above the pad — two of them above 3,200 ft** — so the DATA half was right. The conclusion was wrong. `landedInRecord(metrics)` is already `false` on every one of them, and every surface branches on it: the tile reads *"Last fix from pad"* (`components/GroundTrack.tsx:717`), the button reads *"Copy the last-fix coordinates"* (`:747`), the GPX waypoint is named **`Last fix (record ends in the air)`** (`lib/gps.ts:180`), the `aria-label` branches (`:583`), and the prose says *"This record ends before the ground… not where the rocket came down, and not a direction to walk"* (`:792`). The guard's own docstring (`:76-79`) cites **the same 3,548 ft flight this probe rediscovered** as the reason it exists. So Debrief is AHEAD of the field on this specific point: Eggtimer's LCD shows *"time since last fix"* and leaves the reading to the flyer, where Debrief refuses to call it a landing at all. **What is genuinely still a GAP, and it is the smaller, real one:** the not-landed branch never says HOW HIGH the last fix was, and 3,548 ft of unrecorded descent is a very different search radius from 30 ft. **The quality-field half stands and is untouched by the refutation.** Featherweight's GPS page draws a per-satellite signal histogram against a published dB-Hz→accuracy table — Orange `<24` = *"Weak, can result in 10s of meters horiz. error"*, Blue `>40` = *"Full accuracy available"*, with *"Good GPS performance will have 10 or more satellites"* — and uses it as a pre-flight airframe diagnostic. AltosUI's Table tab is documented as carrying *"horizontal dilution of precision information, and information about the signal being received from the satellites"*. Debrief shows neither, and the bins are already in this repo's own committed fixture (`lib/parsers/__fixtures__/featherweight-gps.csv`), so this is reading a vendor's quality field the vendor's own way — the standard row 43 already holds. **The honest limit, kept so nobody oversells it:** on `altusmetrum-telemetrum.csv` the existing `nsat > 0` gate already blanks all 112 rows with `hdop > 5` and the 478 kept fixes run `hdop` 0.8–1.1, so HDOP buys **graded confidence, not extra filtering**, and must not be shipped as a correctness fix. ~~**One internal inconsistency to settle first:** AltOS 2D fixes keep their position while Featherweight 2D rows are dropped whole — one rule, two answers.~~ **SETTLED AND SHIPPED 2026-08-18 (`0df7eea`).** `lib/gpsFix.ts` states the rule once and all four parsers read it: a position survives a two-dimensional fix, the receiver's own altitude does not, and silence about fix quality grades as three-dimensional. The grade a file states is now a `gpsFixGrade` channel — read off Featherweight's own `FIX` column, derived from AltOS's satellite count with a label that says which. Held by `lib/gpsFix.test.ts`, whose Featherweight case had to WRITE a `FIX == 2` row because no file in reach carries one, and which counts two-dimensional **solutions** rather than rows: the one corpus flight that spends real time on three satellites reads 13 in the CSV its board exported and 13 in the raw download that CSV came from, where a row count read 371 and 13. **No fix moved** across the 12 corpus recordings carrying a position. **Three things this settling turned up that this row had wrong or missing.** (a) **Featherweight's satellite count is not satellites in the fix** — it is satellites the receiver can HEAR, and the ground-station recording carries ten rows whose own `FIX` column says NO FIX while that count reads 16, 18 or 19; publishing it under the `satellites` ChannelKind (whose contract is *0 means the position beside it is held over*) would have made a held-over position claim to be measured. It stays `other`, labelled *Satellites tracked*. (b) **The `>40`/`>32`/`>24` columns are not what this repo's own comment said they were.** Measured over the 174 rows of the jan10 tracker log: `>40 + >32 + >24 == #SATS` holds on **0** rows and `<= #SATS` on **all 174** — three DISJOINT bands whose shortfall is the satellites below 24 dB-Hz — and they are not cumulative thresholds either, since `>32` exceeds `>24` on 127 of the 174. Any future weighting built on them has to start from that. (c) **`pdop`/`hdop`/`vdop` carry an INT32_MAX sentinel** (`2147483647`) meaning *never supplied*, on 2 of the 8 AltOS CSVs that have the columns — so a naive read publishes `2,147,483,647` as a dilution of precision. **And the honest-limit sentence below generalises from ONE file and should not.** Corpus-wide the kept fixes run `hdop` **0.5–5.4**, not the 0.8–1.1 of `altusmetrum-telemetrum.csv` alone. The conclusion survives — graded confidence, not filtering — but the range behind it is wider than stated. **`UNVERIFIED`:** MissileWorks' HDOP display on the LCDT (manual is download-only); the Blue Raven app entirely. **RESOLVED on the dilution half, 2026-08-18 (D12 slice 2).** All three AltOS columns are read; 6 corpus recordings carry them, 16 channels, 5 with a track to state it against. AltosUI's Table tab shows the same field, so this is parity rather than a lead — what Debrief adds is that the number is stated as a RANGE with a middle and is never turned into metres, which no vendor publishes a function for. **Two conventions had to be measured because no manual states them**, and the second corrects what this row said on 2026-08-17: the `2147483647` sentinel is a PER-COLUMN statement, not a per-file one (`intrepid2` supplies `pdop` at 1.60–1.70 while marking `hdop` and `vdop` never-supplied on all 346 of its rows); and a no-fix row carries a held-over dilution that **cannot be recognised by its value** — `endurance` writes an absurd 23.10 on its 108 no-fix samples, but the 121 km TeleMega writes an entirely ordinary 3.60/8.00/8.80 on 12,931 of them, satisfying `PDOP² = HDOP² + VDOP²` to 0.31%. The test is the missing fix, never the number. **The honest-limit sentence above is now measured against what Debrief READS rather than against the raw corpus**: the worst dilution reaching any channel is **6.10**, not the 12.10 this row's neighbours briefly claimed — 12.10 sits in a file whose sheet-export headers fall to the column mapper, which offers no dilution role at all. That mapper gap is D12 slice 4 and is the next thing here; the Featherweight dB-Hz bins are slice 3, and the vendor's own manual warns against reading the weakest satellites as an accuracy signal, so they are a diagnostic to SHOW and never a number to compute an accuracy from. **Sources:** <https://www.featherweightaltimeters.com/uploads/1/0/9/5/109510427/gps_tracker_manual_2021oct25.pdf> · <https://eggtimerrocketry.com/wp-content/uploads/2021/05/Eggfinder-LCD-Users-Guide-2_01c1.pdf> · <https://altusmetrum.org/AltOS/doc/altusmetrum.html> |
| 48 | **Featherweight UI / wFIP** (VERIFIED, 2025-02-20 tracker manual) · **AltosUI** (VERIFIED, AltOS docs) · **Eggtimer/Eggfinder LCD** (VERIFIED, LCD Users Guide 2.01c) · a spreadsheet | **Whether a tool tells a flyer that the maximum it is showing them is NOT the reading it publishes** — the gap between a trace's own peak and the summit figure beside it | `lib/explore.ts` → each altitude trace carries a `scope` stating, in a percentage, how far its own maximum sits above the apogee Debrief reports, on the 3 of 39 corpus records where the two differ and on none of the other 36. It travels into `Copy these stats` and the plotted-data `.csv` through the column header the two already share | **`BETTER`, and the row exists because Debrief had the defect first** | **Added 2026-08-18, resolving itself in the same run.** The opening corpus sweep found the channel explorer publishing **12,060 ft** as the Altitude (AGL) `max` on `blueraven__trf-lemiv-l3` while the Apogee tile beside it read **11,765.5 ft** — 295 ft, 2.51%, in a table whose own code comment calls these *"the numbers a cert document quotes"*, and the board's own summary, its GPS, an Eggtimer on the same flight and the L3 cert paperwork all agree with the tile. Both behaviours underneath are correct and stay: the analysis clamps apogee to the last peak the record descends from, and the trace keeps the deployment transient on purpose. What was missing was a sentence joining them where the maximum is published. **The comparison is uneven and the useful half is what the field does instead.** None of these tools reconciles a trace's peak against a separately-adjudicated apogee, because none of them adjudicates one: AltosUI graphs the recorded series and reports the flight's maximum height from the same data (<https://altusmetrum.org/AltOS/doc/altusmetrum.html>), the Eggfinder LCD reports a peak as the largest value it saw, and a spreadsheet's `MAX()` is the whole method. So there is no gap between the two numbers in those tools **because there is only one number** — which is a weaker guarantee, not a stronger one: the transient a Debrief flyer is now warned about is silently the answer everywhere else. The honest limit, stated so nobody oversells the row: this is a DISCLOSURE, not a correction — Debrief's headline apogee was already right on all 39 records, and what changed is what the panel beside it says. `UNVERIFIED`: whether the Featherweight phone app's own summary applies any equivalent post-apogee clamp; the manual documents the displayed peak but not how it is chosen. |
